<!-- Generated by SigMap gen-context.js v6.10.0 -->
<!-- DO NOT EDIT below the marker line — run gen-context.js to regenerate -->

# Code signatures

## todos
```
lnote-backend\vendor\symfony\uid\UuidV4.php:27  # XXX: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
lnote-backend\vendor\symfony\uid\UuidV4.php:30  # XXX: xxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
lnote-backend\vendor\symfony\uid\UuidV4.php:33  # XXX: xxxxx-xxxx-4xxx-xxxx-xxxxxxxxxxxx
lnote-backend\vendor\symfony\uid\UuidV4.php:36  # XXX: xxxxx-xxxx-4xxx-?xxx-xxxxxxxxxxxx
lnote-backend\vendor\symfony\translation\Loader\PoFileLoader.php:88  # TODO: this fails when comments or contexts are added
lnote-backend\vendor\symfony\http-kernel\HttpCache\HttpCache.php:186  # FIXME: catch exceptions and implement a 500 error page here? -> in Varnish, t
lnote-backend\vendor\symfony\http-kernel\HttpCache\Store.php:164  # TODO: the metaStore referenced an entity that doesn't exist in
```

## lnote-backend

### lnote-backend\vendor\laravel\prompts\src\Concerns\Themes.php
```
trait Themes
  static function theme(?string $name = null) → string
  static function addTheme(string $name, array $renderers) → void
  function getRenderer() → callable
  function renderTheme() → string
```

### lnote-backend\vendor\laravel\prompts\src\Themes\Default\PausePromptRenderer.php
```
class PausePromptRenderer
```

### lnote-backend\vendor\laravel\prompts\src\Themes\Default\SpinnerRenderer.php
```
class SpinnerRenderer
```

### lnote-backend\vendor\laravel\prompts\src\Themes\Default\SuggestPromptRenderer.php
```
class SuggestPromptRenderer
  function valueWithCursorAndArrow(SuggestPrompt $prompt, int $maxWidth) → string
  function spaceForDropdown(SuggestPrompt $prompt) → self
  function renderOptions(SuggestPrompt $prompt) → string
  function reservedLines() → int
```

### lnote-backend\vendor\laravel\prompts\src\Progress.php
```
class Progress
  function map(Closure $callback) → array
  function start() → void
  function advance(int $step = 1) → void
  function finish() → void
  function render() → void
  function label(string $label) → static
  function hint(string $hint) → static
  function percentage() → int|float
```

### lnote-backend\vendor\laravel\prompts\src\NumberPrompt.php
```
class NumberPrompt
  function wrapValidation(mixed $validate) → callable
  function increaseValue() → void
  function decreaseValue() → void
  function value() → int|string
  function valueWithCursor(int $maxWidth) → string
```

### lnote-backend\vendor\laravel\prompts\src\Terminal.php
```
class Terminal
  function read() → string
  function setTty(string $mode) → void
  function restoreTty() → void
  function cols() → int
  function lines() → int
  function initDimensions() → void
  function exit() → void
  function exec(string $command) → string
```

### lnote-backend\vendor\laravel\prompts\src\SuggestPrompt.php
```
class SuggestPrompt
  function highlightedValue() → ?string
  function valueWithCursor(int $maxWidth) → string
  function matches() → array
  function visible() → array
  function selectHighlighted() → void
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\Colors.php
```
trait Colors
  function reset(string $text) → string
  function bold(string $text) → string
  function dim(string $text) → string
  function italic(string $text) → string
  function underline(string $text) → string
  function inverse(string $text) → string
  function hidden(string $text) → string
  function strikethrough(string $text) → string
```

### lnote-backend\vendor\laravel\prompts\src\Spinner.php
```
class Spinner
  function spin(Closure $callback) → mixed
  function resetTerminal(bool $originalAsync) → void
  function renderStatically(Closure $callback) → mixed
  function prompt() → never
  function value() → bool
  function eraseRenderedLines() → void
```

### lnote-backend\vendor\laravel\prompts\src\Themes\Default\StreamRenderer.php
```
class StreamRenderer
```

### lnote-backend\vendor\laravel\prompts\src\SelectPrompt.php
```
class SelectPrompt
  function highlightedValue() → int|string|null
  function value() → int|string|null
  function label() → ?string
  function visible() → array
  function isInvalidWhenRequired(mixed $value) → bool
```

### lnote-backend\vendor\laravel\prompts\src\TextPrompt.php
```
class TextPrompt
  function valueWithCursor(int $maxWidth) → string
```

### lnote-backend\vendor\laravel\prompts\src\MultiSelectPrompt.php
```
class MultiSelectPrompt
  function highlightedValue() → int|string|null
  function value() → array
  function labels() → array
  function visible() → array
  function isHighlighted(string $value) → bool
  function isSelected(string $value) → bool
  function toggleAll() → void
  function toggleHighlighted() → void
```

### lnote-backend\vendor\laravel\prompts\src\ConfirmPrompt.php
```
class ConfirmPrompt
  function value() → bool
  function label() → string
```

### lnote-backend\vendor\laravel\prompts\src\Table.php
```
class Table
  function display() → void
  function prompt() → bool
  function value() → bool
```

### lnote-backend\vendor\laravel\prompts\src\Stream.php
```
class Stream
  function append(string $message) → self
  function close() → void
  function lines() → array
  function prompt() → mixed
  function value() → string
  function fadeOut(int $steps = 10) → array
```

### lnote-backend\vendor\laravel\prompts\src\Themes\Default\TitleRenderer.php
```
class TitleRenderer
```

### lnote-backend\vendor\laravel\prompts\src\SearchPrompt.php
```
class SearchPrompt
  function highlightedValue() → int|string|null
  function search() → void
  function valueWithCursor(int $maxWidth) → string
  function matches() → array
  function visible() → array
  function searchValue() → string
  function value() → int|string|null
  function label() → ?string
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\Scrolling.php
```
trait Scrolling
  function initializeScrolling(?int $highlighted = null) → void
  function reduceScrollingToFitTerminal() → void
  function highlight(?int $index) → void
  function highlightPrevious(int $total, bool $allowNull = false) → void
  function highlightNext(int $total, bool $allowNull = false) → void
  function scrollToHighlighted(int $total) → void
```

### lnote-backend\vendor\laravel\prompts\src\Grid.php
```
class Grid
  function display() → void
  function prompt() → bool
  function value() → bool
```

### lnote-backend\vendor\laravel\prompts\src\NotifyPrompt.php
```
class NotifyPrompt
  function prompt() → bool
  function sendMacOS() → bool
  function sendLinux() → bool
  function sendLinuxNotifySend() → bool
  function sendLinuxKDialog() → bool
  function execute(array $command) → bool
  function escapeAppleScript(string $value) → string
  function display() → void
```

### lnote-backend\vendor\laravel\prompts\src\FormStep.php
```
class FormStep
  function run(array $responses, mixed $previousResponse) → mixed
  function shouldRun(array $responses) → bool
  function shouldIgnoreWhenReverting(array $responses) → bool
```

### lnote-backend\vendor\laravel\prompts\src\PasswordPrompt.php
```
class PasswordPrompt
  function masked() → string
  function maskedWithCursor(int $maxWidth) → string
```

### lnote-backend\vendor\laravel\prompts\src\Themes\Default\ClearRenderer.php
```
class ClearRenderer
```

### lnote-backend\vendor\laravel\prompts\src\Themes\Default\Renderer.php
```
class Renderer
  function line(string $message) → self
  function newLine(int $count = 1) → self
  function warning(string $message) → self
  function error(string $message) → self
  function hint(string $message) → self
  function when(mixed $value, callable $callback, ?callable $default = null) → self
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\HasSpinner.php
```
trait HasSpinner
  function spinnerFrame(int $count) → string
```

### lnote-backend\vendor\laravel\prompts\src\PausePrompt.php
```
class PausePrompt
  function value() → bool
```

### lnote-backend\vendor\laravel\prompts\src\Support\Utils.php
```
class Utils
  static function allMatch(array $values, Closure $callback) → bool
  static function last(array $array) → mixed
  static function search(array $array, Closure $callback) → int|string|false
```

### lnote-backend\vendor\laravel\prompts\src\Output\ConsoleOutput.php
```
class ConsoleOutput
  function newLinesWritten() → int
  function doWrite(string $message, bool $newline) → void
  function writeDirectly(string $message) → void
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\Interactivity.php
```
trait Interactivity
  static function interactive(bool $interactive = true) → void
  function default() → mixed
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\Fallback.php
```
trait Fallback
  static function fallbackWhen(bool $condition) → void
  static function shouldFallback() → bool
  static function fallbackUsing(Closure $fallback) → void
  function fallback() → mixed
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\FakesInputOutput.php
```
trait FakesInputOutput
  static function fake(array $keys = []) → void
  static function fakeKeyPresses(array $keys, callable $callable) → void
  static function assertOutputContains(string $string) → void
  static function assertOutputDoesntContain(string $string) → void
  static function assertStrippedOutputContains(string $string) → void
  static function assertStrippedOutputDoesntContain(string $string) → void
  static function content() → string
  static function strippedContent() → string
```

### lnote-backend\vendor\laravel\prompts\src\Note.php
```
class Note
  function display() → void
  function prompt() → bool
  function value() → bool
```

### lnote-backend\vendor\laravel\prompts\src\Support\Logger.php
```
class Logger
  function line(string $message) → void
  function partial(string $chunk) → void
  function commitPartial() → void
  function success(string $message) → void
  function warning(string $message) → void
  function error(string $message) → void
  function label(string $message) → void
  function subLabel(string $message) → void
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\Cursor.php
```
trait Cursor
  function hideCursor() → void
  function showCursor() → void
  function restoreCursor() → void
  function moveCursor(int $x, int $y = 0) → void
  function moveCursorToColumn(int $column) → void
  function moveCursorUp(int $lines) → void
```

### lnote-backend\vendor\laravel\prompts\src\Exceptions\FormRevertedException.php
```
class FormRevertedException
```

### lnote-backend\vendor\laravel\prompts\src\Exceptions\NonInteractiveValidationException.php
```
class NonInteractiveValidationException
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\Erase.php
```
trait Erase
  function eraseLines(int $count) → void
  function eraseDown() → void
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\Events.php
```
trait Events
  function on(string $event, Closure $callback) → void
  function emit(string $event, mixed ...$data) → void
  function clearListeners() → void
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\HasInfo.php
```
trait HasInfo
  function infoText() → string
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\Truncation.php
```
trait Truncation
  function truncate(string $string, int $width) → string
```

### lnote-backend\vendor\laravel\prompts\src\Output\BufferedConsoleOutput.php
```
class BufferedConsoleOutput
  function fetch() → string
  function content() → string
  function doWrite(string $message, bool $newline) → void
  function writeDirectly(string $message) → void
```

### lnote-backend\vendor\laravel\prompts\src\Title.php
```
class Title
  function prompt() → bool
  function display() → void
  function value() → bool
```

### lnote-backend\vendor\laravel\prompts\src\Clear.php
```
class Clear
  function prompt() → bool
  function display() → void
  function value() → bool
```

### lnote-backend\vendor\laravel\prompts\src\Concerns\Termwind.php
```
trait Termwind
  function termwind(string $html)
  function restoreEscapeSequences(string $string)
```

### lnote-backend\vendor\laravel\prompts\README.md
```
h2 Introduction
h2 Official Documentation
h2 Contributing
h2 Code of Conduct
h2 Security Vulnerabilities
h2 License
```

### lnote-backend\vendor\laravel\prompts\src\Themes\Contracts\Scrolling.php
```
interface Scrolling
  function reservedLines() → int
```

### lnote-backend\vendor\nunomaduro\collision\src\ConsoleColor.php
```
class ConsoleColor
  function apply(array|string $style, string $text) → string
  function setForceStyle(bool $forceStyle) → void
  function isStyleForced() → bool
  function setThemes(array $themes) → void
  function addTheme(string $name, array|string $styles) → void
  function getThemes() → array
  function hasTheme(string $name) → bool
  function removeTheme(string $name) → void
```

### lnote-backend\vendor\nunomaduro\collision\src\Adapters\Laravel\CollisionServiceProvider.php
```
class CollisionServiceProvider
  function boot() → void
  function register() → void
  function provides()
```

### lnote-backend\vendor\nunomaduro\collision\src\Contracts\RenderlessEditor.php
```
interface RenderlessEditor
```

### lnote-backend\vendor\nunomaduro\collision\src\Contracts\RenderlessTrace.php
```
interface RenderlessTrace
```

### lnote-backend\vendor\nunomaduro\collision\src\Contracts\SolutionsRepository.php
```
interface SolutionsRepository
  function getFromThrowable(Throwable $throwable) → array
```

### lnote-backend\vendor\nunomaduro\collision\README.md
```
h2 Installation & Usage
h2 Version Compatibility
h2 No adapter
h2 Contributing
h2 License
code-fence bash
code-fence plain
code-fence php
```

### lnote-backend\vendor\nunomaduro\collision\src\Contracts\RenderableOnCollisionEditor.php
```
interface RenderableOnCollisionEditor
  function toCollisionEditor() → Frame
```

### lnote-backend\vendor\symfony\console\Command\Command.php
```
class Command
  static function getDefaultName() → ?string
  static function getDefaultDescription() → ?string
```

### lnote-backend\vendor\symfony\routing\Loader\Configurator\Traits\RouteTrait.php
```
trait RouteTrait
```

### lnote-backend\vendor\symfony\console\Application.php
```
class Application
  function setDispatcher(EventDispatcherInterface $dispatcher) → void
  function setCommandLoader(CommandLoaderInterface $commandLoader) → void
  function getSignalRegistry() → SignalRegistry
  function setSignalsToDispatchEvent(int ...$signalsToDispatchEvent) → void
  function setAlarmInterval(?int $seconds) → void
  function getAlarmInterval() → ?int
  function run(?InputInterface $input = null, ?OutputInterface $output = null) → int
```

### lnote-backend\vendor\symfony\routing\RouteCompiler.php
```
class RouteCompiler
  static function compile(Route $route) → CompiledRoute
```

### lnote-backend\vendor\symfony\console\Helper\QuestionHelper.php
```
class QuestionHelper
  function ask(InputInterface $input, OutputInterface $output, Question $question) → mixed
  function getName() → string
  static function disableStty() → void
```

### lnote-backend\vendor\symfony\routing\RequestContext.php
```
class RequestContext
  static function fromUri(string $uri, string $host = 'localhost', string $scheme = 'http', int $httpPort = 80, int $httpsPort = 443) → self
```

### lnote-backend\vendor\symfony\routing\Matcher\Dumper\CompiledUrlMatcherDumper.php
```
class CompiledUrlMatcherDumper
  function dump(array $options = []) → string
  function addExpressionLanguageProvider(ExpressionFunctionProviderInterface $provider) → void
  function getCompiledRoutes(bool $forDump = false) → array
```

### lnote-backend\vendor\symfony\routing\Loader\Configurator\CollectionConfigurator.php
```
class CollectionConfigurator
```

### lnote-backend\vendor\symfony\console\Helper\Table.php
```
class Table
  static function setStyleDefinition(string $name, TableStyle $style) → void
  static function getStyleDefinition(string $name) → TableStyle
  function setStyle(TableStyle|string $name) → static
  function getStyle() → TableStyle
  function setColumnStyle(int $columnIndex, TableStyle|string $name) → static
  function getColumnStyle(int $columnIndex) → TableStyle
  function setColumnWidth(int $columnIndex, int $width) → static
  function setColumnWidths(array $widths) → static
```

### lnote-backend\vendor\symfony\console\Input\ArgvInput.php
```
class ArgvInput
  function setTokens(array $tokens) → void
  function parse() → void
  function parseToken(string $token, bool $parseOptions) → bool
```

### lnote-backend\vendor\symfony\console\Attribute\Option.php
```
class Option
  static function tryFrom(\ReflectionParameter|\ReflectionProperty $member) → ?self
```

### lnote-backend\vendor\symfony\console\Resources\completion.bash
```
# This file is part of the Symfony package.
```

### lnote-backend\vendor\symfony\routing\Loader\XmlFileLoader.php
```
class XmlFileLoader
  function load(mixed $file, ?string $type = null) → RouteCollection
  function parseNode(RouteCollection $collection, \DOMElement $node, string $path, string $file) → void
  function supports(mixed $resource, ?string $type = null) → bool
  function parseRoute(RouteCollection $collection, \DOMElement $node, string $path) → void
```

### lnote-backend\vendor\symfony\routing\Matcher\Dumper\CompiledUrlMatcherTrait.php
```
trait CompiledUrlMatcherTrait
  function match(string $pathinfo) → array
```

### lnote-backend\vendor\symfony\routing\Loader\Configurator\ImportConfigurator.php
```
class ImportConfigurator
```

### lnote-backend\vendor\symfony\console\Command\LockableTrait.php
```
trait LockableTrait
```

### lnote-backend\vendor\symfony\console\Input\StringInput.php
```
class StringInput
```

### lnote-backend\vendor\symfony\console\Resources\completion.zsh
```
# compdef {{ COMMAND_NAME }}
```

### lnote-backend\vendor\symfony\console\Command\InvokableCommand.php
```
class InvokableCommand
  function configure(InputDefinition $definition) → void
  function getCode() → callable
```

### lnote-backend\vendor\symfony\console\DependencyInjection\AddConsoleCommandPass.php
```
class AddConsoleCommandPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\routing\Loader\AttributeClassLoader.php
```
class AttributeClassLoader
  function setRouteAnnotationClass(string $class) → void
  function setRouteAttributeClass(string $class) → void
  function load(mixed $class, ?string $type = null) → RouteCollection
  function addRoute(RouteCollection $collection, object $attr, array $globals, \ReflectionClass $class, \ReflectionMethod $method) → void
```

### lnote-backend\vendor\symfony\console\Descriptor\TextDescriptor.php
```
class TextDescriptor
  function describeInputArgument(InputArgument $argument, array $options = []) → void
  function describeInputOption(InputOption $option, array $options = []) → void
  function describeInputDefinition(InputDefinition $definition, array $options = []) → void
```

### lnote-backend\vendor\symfony\routing\Loader\Configurator\Traits\LocalizedRouteTrait.php
```
trait LocalizedRouteTrait
```

### lnote-backend\vendor\symfony\routing\Loader\Configurator\RoutingConfigurator.php
```
class RoutingConfigurator
```

### lnote-backend\vendor\symfony\routing\Loader\Configurator\Traits\PrefixTrait.php
```
trait PrefixTrait
```

### lnote-backend\vendor\symfony\routing\Loader\YamlFileLoader.php
```
class YamlFileLoader
  function load(mixed $file, ?string $type = null) → RouteCollection
  function supports(mixed $resource, ?string $type = null) → bool
  function parseRoute(RouteCollection $collection, string $name, array $config, string $path) → void
```

### lnote-backend\vendor\symfony\console\Attribute\Ask.php
```
class Ask
  static function tryFrom(\ReflectionParameter|\ReflectionProperty $member, string $name) → ?self
```

### lnote-backend\vendor\symfony\routing\Loader\Configurator\RouteConfigurator.php
```
class RouteConfigurator
```

### lnote-backend\vendor\symfony\console\Question\ConfirmationQuestion.php
```
class ConfirmationQuestion
```

### lnote-backend\vendor\symfony\console\Descriptor\XmlDescriptor.php
```
class XmlDescriptor
  function getInputDefinitionDocument(InputDefinition $definition) → \DOMDocument
  function getCommandDocument(Command $command, bool $short = false) → \DOMDocument
  function getApplicationDocument(Application $application, ?string $namespace = null, bool $short = false) → \DOMDocument
```

### lnote-backend\vendor\symfony\console\Style\SymfonyStyle.php
```
class SymfonyStyle
  function block(string|array $messages, ?string $type = null, ?string $style = null, string $prefix = ' ', bool $padding = false, bool $escape = true) → void
  function title(string $message) → void
  function section(string $message) → void
  function listing(array $elements) → void
  function text(string|array $message) → void
  function comment(string|array $message) → void
  function success(string|array $message) → void
  function error(string|array $message) → void
```

### lnote-backend\vendor\symfony\console\Attribute\AsCommand.php
```
class AsCommand
```

### lnote-backend\vendor\symfony\routing\Generator\UrlGenerator.php
```
class UrlGenerator
  function setContext(RequestContext $context) → void
  function getContext() → RequestContext
  function setStrictRequirements(?bool $enabled) → void
  function isStrictRequirements() → ?bool
  function generate(string $name, array $parameters = [], int $referenceType = self::ABSOLUTE_PATH) → string
  function doGenerate(array $variables, array $defaults, array $requirements, array $tokens, array $parameters, string $name, int $referenceType, array $hostTokens, array $requiredSchemes = []) → string
```

### lnote-backend\vendor\symfony\routing\Loader\Configurator\Traits\HostTrait.php
```
trait HostTrait
```

### lnote-backend\vendor\symfony\console\Input\InputDefinition.php
```
class InputDefinition
  function setDefinition(array $definition) → void
  function setArguments(array $arguments = []) → void
  function addArguments(?array $arguments = []) → void
  function addArgument(InputArgument $argument) → void
  function getArgument(string|int $name) → InputArgument
  function hasArgument(string|int $name) → bool
  function getArguments() → array
  function getArgumentCount() → int
```

### lnote-backend\vendor\symfony\routing\Loader\Configurator\RoutesReference.php
```
class RoutesReference
  static function config(array $config) → array
```

### lnote-backend\vendor\symfony\console\Tester\ApplicationTester.php
```
class ApplicationTester
  function run(array $input, array $options = []) → int
```

### lnote-backend\vendor\symfony\console\Helper\Helper.php
```
class Helper
  function setHelperSet(?HelperSet $helperSet) → void
  function getHelperSet() → ?HelperSet
  static function width(?string $string) → int
```

### lnote-backend\vendor\symfony\console\Helper\TableStyle.php
```
class TableStyle
  function setPaddingChar(string $paddingChar) → static
  function getPaddingChar() → string
  function setHorizontalBorderChars(string $outside, ?string $inside = null) → static
  function setVerticalBorderChars(string $outside, ?string $inside = null) → static
  function getBorderChars() → array
  function setCrossingChars(string $cross, string $topLeft, string $topMid, string $topRight, string $midRight, string $bottomRight, string $bottomMid, string $bottomLeft, string $midLeft, ?string $topLeftBottom = null, ?string $topMidBottom = null, ?string $topRightBottom = null) → static
  function setDefaultCrossingChar(string $char) → self
  function getCrossingChar() → string
```

### lnote-backend\vendor\symfony\routing\Matcher\TraceableUrlMatcher.php
```
class TraceableUrlMatcher
  function getTraces(string $pathinfo) → array
  function getTracesForRequest(Request $request) → array
  function matchCollection(string $pathinfo, RouteCollection $routes) → array
```

### lnote-backend\vendor\symfony\routing\Matcher\Dumper\StaticPrefixCollection.php
```
class StaticPrefixCollection
  function getPrefix() → string
  function getRoutes() → array
  function addRoute(string $prefix, array|self $route) → void
  function populateCollection(RouteCollection $routes) → RouteCollection
```

### lnote-backend\vendor\symfony\routing\Generator\UrlGeneratorInterface.php
```
interface UrlGeneratorInterface
  function generate(string $name, array $parameters = [], int $referenceType = self::ABSOLUTE_PATH) → string
```

### lnote-backend\vendor\symfony\console\Logger\ConsoleLogger.php
```
class ConsoleLogger
  function log($level, $message, array $context = []) → void
  function hasErrored() → bool
```

### lnote-backend\vendor\symfony\console\Descriptor\ReStructuredTextDescriptor.php
```
class ReStructuredTextDescriptor
  function describe(OutputInterface $output, object $object, array $options = []) → void
  function write(string $content, bool $decorated = true) → void
  function describeInputArgument(InputArgument $argument, array $options = []) → void
  function describeInputOption(InputOption $option, array $options = []) → void
  function describeInputDefinition(InputDefinition $definition, array $options = []) → void
  function describeCommand(Command $command, array $options = []) → void
```

### lnote-backend\vendor\symfony\console\Tester\CommandTester.php
```
class CommandTester
  function execute(array $input, array $options = []) → int
```

### lnote-backend\vendor\symfony\routing\Matcher\UrlMatcher.php
```
class UrlMatcher
  function setContext(RequestContext $context) → void
  function getContext() → RequestContext
  function match(string $pathinfo) → array
  function matchRequest(Request $request) → array
  function addExpressionLanguageProvider(ExpressionFunctionProviderInterface $provider) → void
  function matchCollection(string $pathinfo, RouteCollection $routes) → array
```

### lnote-backend\vendor\symfony\console\Input\ArrayInput.php
```
class ArrayInput
  function getFirstArgument() → ?string
  function hasParameterOption(string|array $values, bool $onlyParams = false) → bool
  function getParameterOption(string|array $values, string|bool|int|float|array|null $default = false, bool $onlyParams = false) → mixed
  function parse() → void
```

### lnote-backend\vendor\symfony\console\Terminal.php
```
class Terminal
  static function getColorMode() → AnsiColorMode
  static function setColorMode(?AnsiColorMode $colorMode) → void
  function getWidth() → int
  function getHeight() → int
  static function hasSttyAvailable() → bool
```

### lnote-backend\vendor\symfony\routing\Loader\ObjectLoader.php
```
class ObjectLoader
  function load(mixed $resource, ?string $type = null) → RouteCollection
```

### lnote-backend\vendor\symfony\routing\Generator\CompiledUrlGenerator.php
```
class CompiledUrlGenerator
  function generate(string $name, array $parameters = [], int $referenceType = self::ABSOLUTE_PATH) → string
```

### lnote-backend\vendor\symfony\console\Question\Question.php
```
class Question
  function getQuestion() → string
  function getDefault() → string|bool|int|float|nul
  function isMultiline() → bool
  function setMultiline(bool $multiline) → static
  function getTimeout() → ?int
  function setTimeout(?int $seconds) → static
  function isHidden() → bool
  function setHidden(bool $hidden) → static
```

### lnote-backend\vendor\symfony\console\Helper\ProgressIndicator.php
```
class ProgressIndicator
  function setMessage(?string $message) → void
  function start(string $message) → void
  function advance() → void
  function finish(string $message) → void
  static function getFormatDefinition(string $name) → ?string
  static function setPlaceholderFormatterDefinition(string $name, callable $callable) → void
```

### lnote-backend\vendor\symfony\console\Descriptor\ApplicationDescription.php
```
class ApplicationDescription
  function getNamespaces() → array
  function getCommands() → array
  function getCommand(string $name) → Command
```

### lnote-backend\vendor\symfony\console\Helper\SymfonyQuestionHelper.php
```
class SymfonyQuestionHelper
  function writePrompt(OutputInterface $output, Question $question) → void
  function writeError(OutputInterface $output, \Exception $error) → void
```

### lnote-backend\vendor\symfony\routing\Router.php
```
class Router
  function setOptions(array $options) → void
  function setOption(string $key, mixed $value) → void
  function getOption(string $key) → mixed
  function getRouteCollection() → RouteCollection
  function setContext(RequestContext $context) → void
  function getContext() → RequestContext
  function setConfigCacheFactory(ConfigCacheFactoryInterface $configCacheFactory) → void
  function generate(string $name, array $parameters = [], int $referenceType = self::ABSOLUTE_PATH) → string
```

### lnote-backend\vendor\symfony\routing\Loader\AttributeFileLoader.php
```
class AttributeFileLoader
  function load(mixed $file, ?string $type = null) → ?RouteCollection
  function supports(mixed $resource, ?string $type = null) → bool
  function findClass(string $file) → string|false
```

### lnote-backend\vendor\symfony\console\Formatter\OutputFormatter.php
```
class OutputFormatter
  static function escape(string $text) → string
  static function escapeTrailingBackslash(string $text) → string
  function setDecorated(bool $decorated) → void
  function isDecorated() → bool
  function setStyle(string $name, OutputFormatterStyleInterface $style) → void
  function hasStyle(string $name) → bool
  function getStyle(string $name) → OutputFormatterStyleInter
  function format(?string $message) → ?string
```

### lnote-backend\vendor\symfony\console\Output\StreamOutput.php
```
class StreamOutput
  function getStream()
  function doWrite(string $message, bool $newline) → void
  function hasColorSupport() → bool
```

### lnote-backend\vendor\symfony\routing\Matcher\CompiledUrlMatcher.php
```
class CompiledUrlMatcher
```

### lnote-backend\vendor\symfony\routing\Generator\Dumper\CompiledUrlGeneratorDumper.php
```
class CompiledUrlGeneratorDumper
  function getCompiledRoutes() → array
  function getCompiledAliases() → array
  function dump(array $options = []) → string
```

### lnote-backend\vendor\symfony\routing\Matcher\RedirectableUrlMatcher.php
```
class RedirectableUrlMatcher
  function match(string $pathinfo) → array
```

### lnote-backend\vendor\symfony\console\Resources\completion.fish
```
# This file is part of the Symfony package.
```

### lnote-backend\vendor\symfony\routing\Attribute\Route.php
```
class Route
  function setPath(string $path) → void
  function getPath() → ?string
  function setLocalizedPaths(array $localizedPaths) → void
  function getLocalizedPaths() → array
  function setHost(string $pattern) → void
  function getHost() → ?string
  function setName(string $name) → void
  function getName() → ?string
```

### lnote-backend\vendor\symfony\console\Attribute\Argument.php
```
class Argument
  static function tryFrom(\ReflectionParameter|\ReflectionProperty $member) → ?self
  function toInputArgument() → InputArgument
  function resolveValue(InputInterface $input) → mixed
  function getInteractiveAttribute() → ?InteractiveAttributeInte
```

### lnote-backend\vendor\symfony\console\Input\InputOption.php
```
class InputOption
  function getShortcut() → ?string
  function getName() → string
  function acceptValue() → bool
  function isValueRequired() → bool
  function isValueOptional() → bool
  function isArray() → bool
  function isNegatable() → bool
  function setDefault(string|bool|int|float|array|null $default) → void
```

### lnote-backend\vendor\symfony\routing\Loader\AttributeDirectoryLoader.php
```
class AttributeDirectoryLoader
  function load(mixed $path, ?string $type = null) → ?RouteCollection
  function supports(mixed $resource, ?string $type = null) → bool
```

### lnote-backend\vendor\symfony\console\Descriptor\JsonDescriptor.php
```
class JsonDescriptor
  function describeInputArgument(InputArgument $argument, array $options = []) → void
  function describeInputOption(InputOption $option, array $options = []) → void
  function describeInputDefinition(InputDefinition $definition, array $options = []) → void
  function describeCommand(Command $command, array $options = []) → void
  function describeApplication(Application $application, array $options = []) → void
```

### lnote-backend\vendor\symfony\console\Helper\ProcessHelper.php
```
class ProcessHelper
  function run(OutputInterface $output, array|Process $cmd, ?string $error = null, ?callable $callback = null, int $verbosity = OutputInterface::VERBOSITY_VERY_VERBOSE) → Process
  function mustRun(OutputInterface $output, array|Process $cmd, ?string $error = null, ?callable $callback = null, int $verbosity = OutputInterface::VERBOSITY_VERY_VERBOSE) → Process
  function wrapCallback(OutputInterface $output, Process $process, ?callable $callback = null) → callable
  function getName() → string
```

### lnote-backend\vendor\symfony\console\Tester\CommandCompletionTester.php
```
class CommandCompletionTester
  function complete(array $input) → array
```

### lnote-backend\vendor\symfony\routing\Loader\PhpFileLoader.php
```
class PhpFileLoader
  function load(mixed $file, ?string $type = null) → RouteCollection
  function supports(mixed $resource, ?string $type = null) → bool
  function callConfigurator(callable $callback, string $path, string $file) → RouteCollection
```

### lnote-backend\vendor\symfony\console\Output\ConsoleSectionOutput.php
```
class ConsoleSectionOutput
  function setMaxHeight(int $maxHeight) → void
  function clear(?int $lines = null) → void
  function overwrite(string|iterable $message) → void
  function getContent() → string
  function getVisibleContent() → string
  function addContent(string $input, bool $newline = true) → int
  function addNewLineOfInputSubmit() → void
  function doWrite(string $message, bool $newline) → void
```

### lnote-backend\vendor\symfony\console\Helper\TableSeparator.php
```
class TableSeparator
```

### lnote-backend\vendor\symfony\console\Question\ChoiceQuestion.php
```
class ChoiceQuestion
  function getChoices() → array
  function setMultiselect(bool $multiselect) → static
  function isMultiselect() → bool
  function getPrompt() → string
  function setPrompt(string $prompt) → static
  function setErrorMessage(string $errorMessage) → static
```

### lnote-backend\vendor\symfony\console\Descriptor\Descriptor.php
```
class Descriptor
  function describe(OutputInterface $output, object $object, array $options = []) → void
  function write(string $content, bool $decorated = false) → void
```

### lnote-backend\vendor\symfony\console\Descriptor\MarkdownDescriptor.php
```
class MarkdownDescriptor
  function describe(OutputInterface $output, object $object, array $options = []) → void
  function write(string $content, bool $decorated = true) → void
  function describeInputArgument(InputArgument $argument, array $options = []) → void
  function describeInputOption(InputOption $option, array $options = []) → void
  function describeInputDefinition(InputDefinition $definition, array $options = []) → void
  function describeCommand(Command $command, array $options = []) → void
```

### lnote-backend\vendor\symfony\console\Command\ListCommand.php
```
class ListCommand
  function configure() → void
  function execute(InputInterface $input, OutputInterface $output) → int
```

### lnote-backend\vendor\symfony\console\Tester\TesterTrait.php
```
trait TesterTrait
  function getDisplay(bool $normalize = false) → string
  function getErrorOutput(bool $normalize = false) → string
  function getInput() → InputInterface
  function getOutput() → OutputInterface
  function getStatusCode() → int
  function assertCommandIsSuccessful(string $message = '') → void
  function setInputs(array $inputs) → static
```

### lnote-backend\vendor\symfony\routing\Alias.php
```
class Alias
  function withId(string $id) → static
  function getId() → string
  function setDeprecated(string $package, string $version, string $message) → static
```

### lnote-backend\vendor\symfony\console\EventListener\ErrorListener.php
```
class ErrorListener
  function onConsoleError(ConsoleErrorEvent $event) → void
  function onConsoleTerminate(ConsoleTerminateEvent $event) → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\console\Exception\MissingInputException.php
```
class MissingInputException
```

### lnote-backend\vendor\symfony\console\Exception\NamespaceNotFoundException.php
```
class NamespaceNotFoundException
```

### lnote-backend\vendor\symfony\routing\Exception\NoConfigurationException.php
```
class NoConfigurationException
```

### lnote-backend\vendor\symfony\routing\Exception\RouteCircularReferenceException.php
```
class RouteCircularReferenceException
```

### lnote-backend\vendor\symfony\routing\Loader\Configurator\AliasConfigurator.php
```
class AliasConfigurator
  function deprecate(string $package, string $version, string $message) → static
```

### lnote-backend\vendor\symfony\routing\DependencyInjection\RoutingResolverPass.php
```
class RoutingResolverPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\console\Output\ConsoleOutput.php
```
class ConsoleOutput
  function section() → ConsoleSectionOutput
  function setDecorated(bool $decorated) → void
  function setFormatter(OutputFormatterInterface $formatter) → void
  function setVerbosity(int $level) → void
  function getErrorOutput() → OutputInterface
  function setErrorOutput(OutputInterface $error) → void
  function hasStdoutSupport() → bool
  function hasStderrSupport() → bool
```

### lnote-backend\vendor\symfony\routing\Matcher\UrlMatcherInterface.php
```
interface UrlMatcherInterface
  function match(string $pathinfo) → array
```

### lnote-backend\vendor\symfony\console\Helper\DebugFormatterHelper.php
```
class DebugFormatterHelper
  function start(string $id, string $message, string $prefix = 'RUN') → string
  function progress(string $id, string $buffer, bool $error = false, string $prefix = 'OUT', string $errorPrefix = 'ERR') → string
  function stop(string $id, string $message, bool $successful, string $prefix = 'RES') → string
  function getName() → string
```

### lnote-backend\vendor\symfony\console\Output\TrimmedBufferOutput.php
```
class TrimmedBufferOutput
  function fetch() → string
  function doWrite(string $message, bool $newline) → void
```

### lnote-backend\vendor\symfony\console\CI\GithubActionReporter.php
```
class GithubActionReporter
  static function isGithubActionEnvironment() → bool
  function error(string $message, ?string $file = null, ?int $line = null, ?int $col = null) → void
  function warning(string $message, ?string $file = null, ?int $line = null, ?int $col = null) → void
  function debug(string $message, ?string $file = null, ?int $line = null, ?int $col = null) → void
```

### lnote-backend\vendor\symfony\console\Input\Input.php
```
class Input
  function bind(InputDefinition $definition) → void
  function validate() → void
  function isInteractive() → bool
  function setInteractive(bool $interactive) → void
  function getArguments() → array
  function getArgument(string $name) → mixed
  function setArgument(string $name, mixed $value) → void
  function hasArgument(string $name) → bool
```

### lnote-backend\vendor\symfony\routing\Loader\Configurator\Traits\AddTrait.php
```
trait AddTrait
  function add(string $name, string|array $path) → RouteConfigurator
  function alias(string $name, string $alias) → AliasConfigurator
```

### lnote-backend\vendor\symfony\console\Command\HelpCommand.php
```
class HelpCommand
  function configure() → void
  function setCommand(Command $command) → void
  function execute(InputInterface $input, OutputInterface $output) → int
```

### lnote-backend\vendor\symfony\routing\Matcher\RequestMatcherInterface.php
```
interface RequestMatcherInterface
  function matchRequest(Request $request) → array
```

### lnote-backend\vendor\symfony\console\Completion\Output\FishCompletionOutput.php
```
class FishCompletionOutput
  function write(CompletionSuggestions $suggestions, OutputInterface $output) → void
```

### lnote-backend\vendor\symfony\console\Completion\Output\ZshCompletionOutput.php
```
class ZshCompletionOutput
  function write(CompletionSuggestions $suggestions, OutputInterface $output) → void
```

### lnote-backend\vendor\symfony\console\SignalRegistry\SignalMap.php
```
class SignalMap
  static function getSignalName(int $signal) → ?string
```

### lnote-backend\vendor\symfony\routing\DependencyInjection\AddExpressionLanguageProvidersPass.php
```
class AddExpressionLanguageProvidersPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\console\Helper\DescriptorHelper.php
```
class DescriptorHelper
  function describe(OutputInterface $output, ?object $object, array $options = []) → void
  function register(string $format, DescriptorInterface $descriptor) → static
  function getName() → string
  function getFormats() → array
```

### lnote-backend\vendor\symfony\console\SingleCommandApplication.php
```
class SingleCommandApplication
  function setVersion(string $version) → static
  function setAutoExit(bool $autoExit) → static
  function run(?InputInterface $input = null, ?OutputInterface $output = null) → int
```

### lnote-backend\vendor\symfony\console\Helper\TableCell.php
```
class TableCell
  function getColspan() → int
  function getRowspan() → int
  function getStyle() → ?TableCellStyle
```

### lnote-backend\vendor\symfony\console\Input\InputArgument.php
```
class InputArgument
  function getName() → string
  function isRequired() → bool
  function isArray() → bool
  function setDefault(string|bool|int|float|array|null $default) → void
  function getDefault() → string|bool|int|float|arr
  function hasCompletion() → bool
  function complete(CompletionInput $input, CompletionSuggestions $suggestions) → void
  function getDescription() → string
```

### lnote-backend\vendor\symfony\routing\Loader\DirectoryLoader.php
```
class DirectoryLoader
  function load(mixed $file, ?string $type = null) → mixed
  function supports(mixed $resource, ?string $type = null) → bool
```

### lnote-backend\vendor\symfony\console\Attribute\Interact.php
```
class Interact
  static function tryFrom(\ReflectionMethod $method) → ?self
  function getFunction(object $instance) → \ReflectionFunction
```

### lnote-backend\vendor\symfony\routing\Generator\ConfigurableRequirementsInterface.php
```
interface ConfigurableRequirementsInterface
  function setStrictRequirements(?bool $enabled) → void
  function isStrictRequirements() → ?bool
```

### lnote-backend\vendor\symfony\console\Completion\Output\BashCompletionOutput.php
```
class BashCompletionOutput
  function write(CompletionSuggestions $suggestions, OutputInterface $output) → void
```

### lnote-backend\vendor\symfony\console\Helper\TableCellStyle.php
```
class TableCellStyle
  function getOptions() → array
  function getTagOptions() → array
  function getPadByAlign() → int
  function getCellFormat() → ?string
```

### lnote-backend\vendor\symfony\routing\Matcher\ExpressionLanguageProvider.php
```
class ExpressionLanguageProvider
  function getFunctions() → array
  function get(string $function) → callable
```

### lnote-backend\vendor\symfony\routing\Generator\Dumper\GeneratorDumper.php
```
class GeneratorDumper
  function getRoutes() → RouteCollection
```

### lnote-backend\vendor\symfony\routing\Matcher\Dumper\MatcherDumper.php
```
class MatcherDumper
  function getRoutes() → RouteCollection
```

### lnote-backend\vendor\symfony\console\Helper\FormatterHelper.php
```
class FormatterHelper
  function formatSection(string $section, string $message, string $style = 'info') → string
  function formatBlock(string|array $messages, string $style, bool $large = false) → string
  function truncate(string $message, int $length, string $suffix = '...') → string
  function getName() → string
```

### lnote-backend\vendor\symfony\console\Output\Output.php
```
class Output
  function setFormatter(OutputFormatterInterface $formatter) → void
  function getFormatter() → OutputFormatterInterface
  function setDecorated(bool $decorated) → void
  function isDecorated() → bool
  function setVerbosity(int $level) → void
  function getVerbosity() → int
  function isSilent() → bool
  function isQuiet() → bool
```

### lnote-backend\vendor\symfony\console\Helper\InputAwareHelper.php
```
class InputAwareHelper
  function setInput(InputInterface $input) → void
```

### lnote-backend\vendor\symfony\console\Input\InputInterface.php
```
interface InputInterface
  function getFirstArgument() → ?string
  function hasParameterOption(string|array $values, bool $onlyParams = false) → bool
  function getParameterOption(string|array $values, string|bool|int|float|array|null $default = false, bool $onlyParams = false) → mixed
  function bind(InputDefinition $definition) → void
  function validate() → void
  function getArguments() → array
  function getArgument(string $name) → mixed
  function setArgument(string $name, mixed $value) → void
```

### lnote-backend\vendor\symfony\routing\Matcher\RedirectableUrlMatcherInterface.php
```
interface RedirectableUrlMatcherInterface
  function redirect(string $path, string $route, ?string $scheme = null) → array
```

### lnote-backend\vendor\symfony\console\Formatter\OutputFormatterStyleStack.php
```
class OutputFormatterStyleStack
  function reset() → void
  function push(OutputFormatterStyleInterface $style) → void
  function pop(?OutputFormatterStyleInterface $style = null) → OutputFormatterStyleInter
  function getCurrent() → OutputFormatterStyleInter
  function setEmptyStyle(OutputFormatterStyleInterface $emptyStyle) → static
  function getEmptyStyle() → OutputFormatterStyleInter
```

### lnote-backend\vendor\symfony\routing\README.md
```
code-fence bash
code-fence plain
code-fence php
code-fence sponsor
```

### lnote-backend\vendor\symfony\routing\RouteCompilerInterface.php
```
interface RouteCompilerInterface
  static function compile(Route $route) → CompiledRoute
```

### lnote-backend\vendor\symfony\console\Event\ConsoleEvent.php
```
class ConsoleEvent
  function getCommand() → ?Command
  function getInput() → InputInterface
  function getOutput() → OutputInterface
```

### lnote-backend\vendor\symfony\routing\Loader\GlobFileLoader.php
```
class GlobFileLoader
  function load(mixed $resource, ?string $type = null) → mixed
  function supports(mixed $resource, ?string $type = null) → bool
```

### lnote-backend\vendor\symfony\console\Output\BufferedOutput.php
```
class BufferedOutput
  function fetch() → string
  function doWrite(string $message, bool $newline) → void
```

### lnote-backend\vendor\symfony\console\Input\InputAwareInterface.php
```
interface InputAwareInterface
  function setInput(InputInterface $input) → void
```

### lnote-backend\vendor\symfony\routing\Loader\ContainerLoader.php
```
class ContainerLoader
  function supports(mixed $resource, ?string $type = null) → bool
  function getObject(string $id) → object
```

### lnote-backend\vendor\symfony\console\CommandLoader\ContainerCommandLoader.php
```
class ContainerCommandLoader
  function get(string $name) → Command
  function has(string $name) → bool
  function getNames() → array
```

### lnote-backend\vendor\symfony\console\CommandLoader\FactoryCommandLoader.php
```
class FactoryCommandLoader
  function has(string $name) → bool
  function get(string $name) → Command
  function getNames() → array
```

### lnote-backend\vendor\symfony\console\Completion\Output\CompletionOutputInterface.php
```
interface CompletionOutputInterface
  function write(CompletionSuggestions $suggestions, OutputInterface $output) → void
```

### lnote-backend\vendor\symfony\console\Formatter\WrappableOutputFormatterInterface.php
```
interface WrappableOutputFormatterInterface
  function formatAndWrap(?string $message, int $width) → string
```

### lnote-backend\vendor\symfony\routing\Loader\ClosureLoader.php
```
class ClosureLoader
  function load(mixed $closure, ?string $type = null) → RouteCollection
  function supports(mixed $resource, ?string $type = null) → bool
```

### lnote-backend\vendor\symfony\console\Style\OutputStyle.php
```
class OutputStyle
  function newLine(int $count = 1) → void
  function createProgressBar(int $max = 0) → ProgressBar
  function write(string|iterable $messages, bool $newline = false, int $type = self::OUTPUT_NORMAL) → void
  function writeln(string|iterable $messages, int $type = self::OUTPUT_NORMAL) → void
  function setVerbosity(int $level) → void
  function getVerbosity() → int
  function setDecorated(bool $decorated) → void
  function isDecorated() → bool
```

### lnote-backend\vendor\symfony\console\Style\StyleInterface.php
```
interface StyleInterface
  function title(string $message) → void
  function section(string $message) → void
  function listing(array $elements) → void
  function text(string|array $message) → void
  function success(string|array $message) → void
  function error(string|array $message) → void
  function warning(string|array $message) → void
  function note(string|array $message) → void
```

### lnote-backend\vendor\symfony\console\Input\StreamableInputInterface.php
```
interface StreamableInputInterface
  function setStream($stream) → void
  function getStream()
```

### lnote-backend\vendor\symfony\console\Descriptor\DescriptorInterface.php
```
interface DescriptorInterface
  function describe(OutputInterface $output, object $object, array $options = []) → void
```

### lnote-backend\vendor\symfony\console\Command\SignalableCommandInterface.php
```
interface SignalableCommandInterface
  function getSubscribedSignals() → array
  function handleSignal(int $signal, int|false $previousExitCode = 0) → int|false
```

### lnote-backend\vendor\symfony\console\Attribute\Reflection\ReflectionMember.php
```
class ReflectionMember
  function getAttribute(string $class) → ?object
  function getSourceName() → string
  function getSourceThis() → ?object
  function getType() → ?\ReflectionType
  function getName() → string
  function hasDefaultValue() → bool
  function getDefaultValue() → mixed
  function isNullable() → bool
```

### lnote-backend\vendor\symfony\console\Output\OutputInterface.php
```
interface OutputInterface
  function write(string|iterable $messages, bool $newline = false, int $options = 0) → void
  function writeln(string|iterable $messages, int $options = 0) → void
  function setVerbosity(int $level) → void
  function getVerbosity() → int
  function isQuiet() → bool
  function isVerbose() → bool
  function isVeryVerbose() → bool
  function isDebug() → bool
```

### lnote-backend\vendor\symfony\routing\Generator\Dumper\GeneratorDumperInterface.php
```
interface GeneratorDumperInterface
  function dump(array $options = []) → string
  function getRoutes() → RouteCollection
```

### lnote-backend\vendor\symfony\routing\Matcher\Dumper\MatcherDumperInterface.php
```
interface MatcherDumperInterface
  function dump(array $options = []) → string
  function getRoutes() → RouteCollection
```

### lnote-backend\vendor\symfony\console\Formatter\OutputFormatterStyle.php
```
class OutputFormatterStyle
  function setForeground(?string $color) → void
  function setBackground(?string $color) → void
  function setHref(string $url) → void
  function setOption(string $option) → void
  function unsetOption(string $option) → void
  function setOptions(array $options) → void
  function apply(string $text) → string
```

### lnote-backend\vendor\symfony\console\Output\NullOutput.php
```
class NullOutput
  function setFormatter(OutputFormatterInterface $formatter) → void
  function getFormatter() → OutputFormatterInterface
  function setDecorated(bool $decorated) → void
  function isDecorated() → bool
  function setVerbosity(int $level) → void
  function getVerbosity() → int
  function isSilent() → bool
  function isQuiet() → bool
```

### lnote-backend\vendor\symfony\console\Attribute\InteractiveAttributeInterface.php
```
interface InteractiveAttributeInterface
  function getFunction(object $instance) → \ReflectionFunction
```

### lnote-backend\vendor\symfony\routing\Attribute\DeprecatedAlias.php
```
class DeprecatedAlias
  function getMessage() → string
  function getAliasName() → string
  function getPackage() → string
  function getVersion() → string
```

### lnote-backend\vendor\symfony\console\CommandLoader\CommandLoaderInterface.php
```
interface CommandLoaderInterface
  function get(string $name) → Command
  function has(string $name) → bool
  function getNames() → array
```

### lnote-backend\vendor\symfony\console\Helper\HelperInterface.php
```
interface HelperInterface
  function setHelperSet(?HelperSet $helperSet) → void
  function getHelperSet() → ?HelperSet
  function getName() → string
```

### lnote-backend\vendor\symfony\routing\RequestContextAwareInterface.php
```
interface RequestContextAwareInterface
  function setContext(RequestContext $context) → void
  function getContext() → RequestContext
```

### lnote-backend\vendor\symfony\console\Output\ConsoleOutputInterface.php
```
interface ConsoleOutputInterface
  function getErrorOutput() → OutputInterface
  function setErrorOutput(OutputInterface $error) → void
  function section() → ConsoleSectionOutput
```

### lnote-backend\vendor\symfony\console\Formatter\OutputFormatterInterface.php
```
interface OutputFormatterInterface
  function setDecorated(bool $decorated) → void
  function isDecorated() → bool
  function setStyle(string $name, OutputFormatterStyleInterface $style) → void
  function hasStyle(string $name) → bool
  function getStyle(string $name) → OutputFormatterStyleInter
  function format(?string $message) → ?string
```

### lnote-backend\vendor\symfony\console\Formatter\OutputFormatterStyleInterface.php
```
interface OutputFormatterStyleInterface
  function setForeground(?string $color) → void
  function setBackground(?string $color) → void
  function setOption(string $option) → void
  function unsetOption(string $option) → void
  function setOptions(array $options) → void
  function apply(string $text) → string
```

### lnote-backend\vendor\voku\portable-ascii\README.md
```
h1 🔡 Portable ASCII
h2 Description
h2 Index
h2 Alternative
h2 Install "Portable ASCII" via "composer require"
h2 Why Portable ASCII?[]()
h2 Requirements and Recommendations
h2 Usage
h1 Portable ASCII | API
h2 Class methods
h4 charsArray(bool $replace_extra_symbols): array
h4 charsArrayWithMultiLanguageValues(bool $replace_extra_symbols): array
h4 charsArrayWithOneLanguage(string $language, bool $replace_extra_symbols, bool $asOrigReplaceArray): array
h4 charsArrayWithSingleLanguageValues(bool $replace_extra_symbols, bool $asOrigReplaceArray): array
h4 clean(string $str, bool $normalize_whitespace, bool $keep_non_breaking_space, bool $normalize_msword, bool $remove_invisible_characters): string
h4 getAllLanguages(): string[]
h4 is_ascii(string $str): bool
h4 normalize_msword(string $str): string
h4 normalize_whitespace(string $str, bool $keepNonBreakingSpace, bool $keepBidiUnicodeControls, bool $normalize_control_characters): string
h4 remove_invisible_characters(string $str, bool $url_encoded, string $replacement, bool $keep_basic_control_characters): string
h4 to_ascii(string $str, string $language, bool $remove_unsupported_chars, bool $replace_extra_symbols, bool $use_transliterate, bool|null $replace_single_chars_only): string
h4 to_ascii_remap(string $str1, string $str2): string[]
h4 to_filename(string $str, bool $use_transliterate, string $fallback_char): string
h4 to_slugify(string $str, string $separator, string $language, string[] $replacements, bool $replace_extra_symbols, bool $use_str_to_lower, bool $use_transliterate): string
h4 to_transliterate(string $str, string|null $unknown, bool $strict): string
```

### lnote-backend\vendor\voku\portable-ascii\CHANGELOG.md
```
h1 Changelog
h3 2.1.1 (2026-04-24)
h3 2.1.0 (2026-04-16)
h3 2.0.3 (2024-11-21)
h3 2.0.2 (2024-11-21)
h3 2.0.1 (2022-03-08)
h3 2.0.0 (2022-01-24)
h3 1.6.1 (2022-01-24)
h3 1.6.0 (2022-01-24)
h3 1.5.6 (2020-11-12)
h3 1.5.5 (2020-11-12)
h3 1.5.4 (2020-11-08)
h3 1.5.3 (2020-07-23)
h3 1.5.2 (2020-06-16)
h3 1.5.1 (2020-05-26)
h3 1.5.0 (2020-05-24)
h3 1.4.11 (2020-05-23)
h3 1.4.10 (2020-03-13)
h3 1.4.9 (2020-03-06)
h3 1.4.8 (2020-02-06)
h3 1.4.7 (2020-01-27)
h3 1.4.6 (2019-12-23)
h3 1.4.5 (2019-12-19)
h3 1.4.4 (2019-12-19)
h3 1.4.3 (2019-12-19)
```

### lnote-backend\vendor\voku\portable-ascii\.deepsource.toml
```
table [[analyzers]]
key version
key name
```

### lnote-backend\vendor\laravel\sail\bin\sail
```
function forward_agent_env() {
```

### lnote-backend\vendor\laravel\sail\src\Console\Concerns\InteractsWithDockerComposeServices.php
```
trait InteractsWithDockerComposeServices
  function gatherServicesInteractively()
  function buildDockerCompose(array $services)
  function replaceEnvVariables(array $services)
```

### lnote-backend\vendor\laravel\sail\src\Console\InstallCommand.php
```
class InstallCommand
  function handle()
```

### lnote-backend\vendor\laravel\sail\src\Console\PublishCommand.php
```
class PublishCommand
  function handle()
```

### lnote-backend\vendor\laravel\sail\src\Console\AddCommand.php
```
class AddCommand
  function handle()
```

### lnote-backend\vendor\laravel\sail\src\SailServiceProvider.php
```
class SailServiceProvider
  function boot()
  function registerCommands()
  function configurePublishing()
  function provides()
```

### lnote-backend\vendor\laravel\sail\runtimes\8.5\Dockerfile
```
FROM ubuntu:24.04
EXPOSE 80/
ENTRYPOINT ["start-container"]
ENV DEBIAN_FRONTEND
ENV TZ
ENV LANG
ENV SUPERVISOR_PHP_COMMAND
ENV SUPERVISOR_PHP_USER
ENV PLAYWRIGHT_BROWSERS_PATH
ARG WWWGROUP
ARG NODE_VERSION
ARG MYSQL_CLIENT
ARG POSTGRES_VERSION
```

### lnote-backend\vendor\laravel\sail\runtimes\8.0\Dockerfile
```
FROM ubuntu:24.04
EXPOSE 80/
ENTRYPOINT ["start-container"]
ENV DEBIAN_FRONTEND
ENV TZ
ENV LANG
ENV SUPERVISOR_PHP_COMMAND
ENV SUPERVISOR_PHP_USER
ARG WWWGROUP
ARG NODE_VERSION
ARG POSTGRES_VERSION
```

### lnote-backend\vendor\laravel\sail\runtimes\8.1\Dockerfile
```
FROM ubuntu:24.04
EXPOSE 80/
ENTRYPOINT ["start-container"]
ENV DEBIAN_FRONTEND
ENV TZ
ENV LANG
ENV SUPERVISOR_PHP_COMMAND
ENV SUPERVISOR_PHP_USER
ARG WWWGROUP
ARG NODE_VERSION
ARG POSTGRES_VERSION
```

### lnote-backend\vendor\laravel\sail\runtimes\8.2\Dockerfile
```
FROM ubuntu:24.04
EXPOSE 80/
ENTRYPOINT ["start-container"]
ENV DEBIAN_FRONTEND
ENV TZ
ENV LANG
ENV SUPERVISOR_PHP_COMMAND
ENV SUPERVISOR_PHP_USER
ARG WWWGROUP
ARG NODE_VERSION
ARG POSTGRES_VERSION
```

### lnote-backend\vendor\laravel\sail\runtimes\8.3\Dockerfile
```
FROM ubuntu:24.04
EXPOSE 80/
ENTRYPOINT ["start-container"]
ENV DEBIAN_FRONTEND
ENV TZ
ENV LANG
ENV SUPERVISOR_PHP_COMMAND
ENV SUPERVISOR_PHP_USER
ENV PLAYWRIGHT_BROWSERS_PATH
ARG WWWGROUP
ARG NODE_VERSION
ARG MYSQL_CLIENT
ARG POSTGRES_VERSION
```

### lnote-backend\vendor\laravel\sail\runtimes\8.4\Dockerfile
```
FROM ubuntu:24.04
EXPOSE 80/
ENTRYPOINT ["start-container"]
ENV DEBIAN_FRONTEND
ENV TZ
ENV LANG
ENV SUPERVISOR_PHP_COMMAND
ENV SUPERVISOR_PHP_USER
ENV PLAYWRIGHT_BROWSERS_PATH
ARG WWWGROUP
ARG NODE_VERSION
ARG MYSQL_CLIENT
ARG POSTGRES_VERSION
```

### lnote-backend\vendor\laravel\sail\README.md
```
h2 Introduction
h4 Inspiration
h2 Official Documentation
h2 Contributing
h2 Code of Conduct
h2 Security Vulnerabilities
h2 License
```

### lnote-backend\vendor\symfony\mime\Encoder\QpEncoder.php
```
class QpEncoder
```

### lnote-backend\vendor\symfony\mime\Email.php
```
class Email
  function subject(string $subject) → static
  function getSubject() → ?string
  function date(\DateTimeInterface $dateTime) → static
  function getDate() → ?\DateTimeImmutable
  function returnPath(Address|string $address) → static
  function getReturnPath() → ?Address
  function sender(Address|string $address) → static
  function getSender() → ?Address
```

### lnote-backend\vendor\symfony\mime\Part\TextPart.php
```
class TextPart
  function getMediaType() → string
  function getMediaSubtype() → string
  function setDisposition(string $disposition) → static
  function getDisposition() → ?string
  function setName(string $name) → static
  function getName() → ?string
  function getBody() → string
  function bodyToString() → string
```

### lnote-backend\vendor\symfony\mime\Part\SMimePart.php
```
class SMimePart
  function getMediaType() → string
  function getMediaSubtype() → string
  function bodyToString() → string
  function bodyToIterable() → iterable
  function getPreparedHeaders() → Headers
```

### lnote-backend\vendor\symfony\mime\Header\AbstractHeader.php
```
class AbstractHeader
  function setCharset(string $charset) → void
  function getCharset() → ?string
  function setLanguage(string $lang) → void
  function getLanguage() → ?string
  function getName() → string
  function setMaxLineLength(int $lineLength) → void
  function getMaxLineLength() → int
  function toString() → string
```

### lnote-backend\vendor\symfony\mime\RawMessage.php
```
class RawMessage
  function toString() → string
  function toIterable() → iterable
  function ensureValidity() → void
```

### lnote-backend\vendor\symfony\mime\Part\DataPart.php
```
class DataPart
  static function fromPath(string $path, ?string $name = null, ?string $contentType = null) → self
  function asInline() → static
  function setContentId(string $cid) → static
  function getContentId() → string
  function hasContentId() → bool
  function getMediaType() → string
  function getPreparedHeaders() → Headers
  function asDebugString() → string
```

### lnote-backend\vendor\symfony\mime\FileBinaryMimeTypeGuesser.php
```
class FileBinaryMimeTypeGuesser
  function isGuesserSupported() → bool
  function guessMimeType(string $path) → ?string
```

### lnote-backend\vendor\symfony\mime\FileinfoMimeTypeGuesser.php
```
class FileinfoMimeTypeGuesser
  function isGuesserSupported() → bool
  function guessMimeType(string $path) → ?string
```

### lnote-backend\vendor\symfony\mime\DraftEmail.php
```
class DraftEmail
  function getPreparedHeaders() → Headers
```

### lnote-backend\vendor\symfony\mime\Crypto\SMime.php
```
class SMime
  function normalizeFilePath(string $path) → string
  function iteratorToFile(iterable $iterator, $stream) → void
  function convertMessageToSMimePart($stream, string $type, string $subtype) → SMimePart
  function getStreamIterator($stream) → iterable
```

### lnote-backend\vendor\symfony\mime\Encoder\Base64Encoder.php
```
class Base64Encoder
  function encodeString(string $string, ?string $charset = 'utf-8', int $firstLineOffset = 0, int $maxLineLength = 0) → string
```

### lnote-backend\vendor\symfony\mime\DependencyInjection\AddMimeTypeGuesserPass.php
```
class AddMimeTypeGuesserPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\mime\Exception\AddressEncoderException.php
```
class AddressEncoderException
```

### lnote-backend\vendor\symfony\mime\Part\AbstractPart.php
```
class AbstractPart
  function getHeaders() → Headers
  function getPreparedHeaders() → Headers
  function toString() → string
  function toIterable() → iterable
  function asDebugString() → string
```

### lnote-backend\vendor\symfony\mime\Message.php
```
class Message
  function setBody(?AbstractPart $body) → static
  function getBody() → ?AbstractPart
  function setHeaders(Headers $headers) → static
  function getHeaders() → Headers
  function getPreparedHeaders() → Headers
  function toString() → string
  function toIterable() → iterable
  function ensureValidity() → void
```

### lnote-backend\vendor\symfony\mime\HtmlToTextConverter\LeagueHtmlToMarkdownConverter.php
```
class LeagueHtmlToMarkdownConverter
  function convert(string $html, string $charset) → string
```

### lnote-backend\vendor\symfony\mime\Encoder\AddressEncoderInterface.php
```
interface AddressEncoderInterface
  function encodeString(string $address) → string
```

### lnote-backend\vendor\symfony\mime\Part\AbstractMultipartPart.php
```
class AbstractMultipartPart
  function getParts() → array
  function getMediaType() → string
  function getPreparedHeaders() → Headers
  function bodyToString() → string
  function bodyToIterable() → iterable
  function asDebugString() → string
```

### lnote-backend\vendor\symfony\mime\Encoder\EncoderInterface.php
```
interface EncoderInterface
  function encodeString(string $string, ?string $charset = 'utf-8', int $firstLineOffset = 0, int $maxLineLength = 0) → string
```

### lnote-backend\vendor\symfony\mime\HtmlToTextConverter\HtmlToTextConverterInterface.php
```
interface HtmlToTextConverterInterface
  function convert(string $html, string $charset) → string
```

### lnote-backend\vendor\symfony\mime\Encoder\MimeHeaderEncoderInterface.php
```
interface MimeHeaderEncoderInterface
  function getName() → string
```

### lnote-backend\vendor\symfony\mime\HtmlToTextConverter\DefaultHtmlToTextConverter.php
```
class DefaultHtmlToTextConverter
  function convert(string $html, string $charset) → string
```

### lnote-backend\vendor\symfony\mime\Part\MessagePart.php
```
class MessagePart
  function getMediaType() → string
  function getMediaSubtype() → string
  function getBody() → string
  function bodyToString() → string
  function bodyToIterable() → iterable
```

### lnote-backend\vendor\symfony\mime\MimeTypeGuesserInterface.php
```
interface MimeTypeGuesserInterface
  function isGuesserSupported() → bool
  function guessMimeType(string $path) → ?string
```

### lnote-backend\vendor\symfony\mime\Header\UnstructuredHeader.php
```
class UnstructuredHeader
  function setBody(mixed $body) → void
  function getBody() → string
  function getValue() → string
  function setValue(string $value) → void
  function getBodyAsString() → string
```

### lnote-backend\vendor\symfony\mime\MimeTypesInterface.php
```
interface MimeTypesInterface
  function getExtensions(string $mimeType) → array
  function getMimeTypes(string $ext) → array
```

### lnote-backend\vendor\symfony\mime\BodyRendererInterface.php
```
interface BodyRendererInterface
  function render(Message $message) → void
```

### lnote-backend\vendor\symfony\mime\Part\File.php
```
class File
  function getPath() → string
  function getContentType() → string
  function getSize() → int
  function getFilename() → string
```

### lnote-backend\vendor\symfony\mime\Encoder\ContentEncoderInterface.php
```
interface ContentEncoderInterface
  function encodeByteStream($stream, int $maxLineLength = 0) → iterable
  function getName() → string
```

### lnote-backend\vendor\symfony\mime\Header\HeaderInterface.php
```
interface HeaderInterface
  function setBody(mixed $body) → void
  function getBody() → mixed
  function setCharset(string $charset) → void
  function getCharset() → ?string
  function setLanguage(string $lang) → void
  function getLanguage() → ?string
  function getName() → string
  function setMaxLineLength(int $lineLength) → void
```

### lnote-backend\vendor\symfony\uid\Uuid.php
```
class Uuid
  static function fromString(string $uuid) → static
  static function isValid(string $uuid) → bool
```

### lnote-backend\vendor\symfony\uid\Command\GenerateUuidCommand.php
```
class GenerateUuidCommand
  function configure() → void
  function execute(InputInterface $input, OutputInterface $output) → int
```

### lnote-backend\vendor\symfony\uid\UuidV7.php
```
class UuidV7
  function getDateTime() → \DateTimeImmutable
  static function generate(?\DateTimeInterface $time = null) → string
```

### lnote-backend\vendor\symfony\uid\UuidV4.php
```
class UuidV4
```

### lnote-backend\vendor\symfony\uid\BinaryUtil.php
```
class BinaryUtil
  static function toBase(string $bytes, array $map) → string
  static function fromBase(string $digits, array $map) → string
  static function add(string $a, string $b) → string
  static function hexToNumericString(string $time) → string
```

### lnote-backend\vendor\symfony\uid\Ulid.php
```
class Ulid
  static function isValid(string $ulid) → bool
  static function fromString(string $ulid) → static
  function toBinary() → string
  function toBase32() → string
  function getDateTime() → \DateTimeImmutable
```

### lnote-backend\vendor\symfony\uid\UuidV3.php
```
class UuidV3
```

### lnote-backend\vendor\symfony\uid\UuidV5.php
```
class UuidV5
```

### lnote-backend\vendor\symfony\uid\Command\GenerateUlidCommand.php
```
class GenerateUlidCommand
  function configure() → void
  function execute(InputInterface $input, OutputInterface $output) → int
  function complete(CompletionInput $input, CompletionSuggestions $suggestions) → void
```

### lnote-backend\vendor\symfony\uid\Command\InspectUuidCommand.php
```
class InspectUuidCommand
  function configure() → void
  function execute(InputInterface $input, OutputInterface $output) → int
```

### lnote-backend\vendor\symfony\uid\UuidV8.php
```
class UuidV8
```

### lnote-backend\vendor\symfony\uid\NilUuid.php
```
class NilUuid
```

### lnote-backend\vendor\symfony\uid\MaxUuid.php
```
class MaxUuid
```

### lnote-backend\vendor\symfony\uid\Command\InspectUlidCommand.php
```
class InspectUlidCommand
  function configure() → void
  function execute(InputInterface $input, OutputInterface $output) → int
```

### lnote-backend\vendor\symfony\uid\Factory\NameBasedUuidFactory.php
```
class NameBasedUuidFactory
  function create(string $name) → UuidV5|UuidV3
```

### lnote-backend\vendor\symfony\uid\MaxUlid.php
```
class MaxUlid
```

### lnote-backend\vendor\symfony\uid\NilUlid.php
```
class NilUlid
```

### lnote-backend\vendor\symfony\uid\Factory\UuidFactory.php
```
class UuidFactory
  function create() → Uuid
  function randomBased() → RandomBasedUuidFactory
  function timeBased(Uuid|string|null $node = null) → TimeBasedUuidFactory
  function nameBased(Uuid|string|null $namespace = null) → NameBasedUuidFactory
```

### lnote-backend\vendor\symfony\uid\Factory\TimeBasedUuidFactory.php
```
class TimeBasedUuidFactory
  function create(?\DateTimeInterface $time = null) → Uuid&TimeBasedUidInterfac
```

### lnote-backend\vendor\symfony\uid\UuidV6.php
```
class UuidV6
  function getDateTime() → \DateTimeImmutable
  function getNode() → string
  function toV7() → UuidV7
  static function generate(?\DateTimeInterface $time = null, ?Uuid $node = null) → string
```

### lnote-backend\vendor\symfony\uid\Factory\MockUuidFactory.php
```
class MockUuidFactory
  function create() → Uuid
  function randomBased() → RandomBasedUuidFactory
  function create() → UuidV4
  function timeBased(Uuid|string|null $node = null) → TimeBasedUuidFactory
  function create(?\DateTimeInterface $time = null) → Uuid&TimeBasedUidInterfac
  function nameBased(Uuid|string|null $namespace = null) → NameBasedUuidFactory
  function create(string $name) → UuidV5|UuidV3
```

### lnote-backend\vendor\symfony\uid\Factory\RandomBasedUuidFactory.php
```
class RandomBasedUuidFactory
  function create() → UuidV4
```

### lnote-backend\vendor\symfony\uid\UuidV1.php
```
class UuidV1
  function getDateTime() → \DateTimeImmutable
  function getNode() → string
  function toV6() → UuidV6
  function toV7() → UuidV7
  static function generate(?\DateTimeInterface $time = null, ?Uuid $node = null) → string
```

### lnote-backend\vendor\symfony\uid\Factory\UlidFactory.php
```
class UlidFactory
  function create(?\DateTimeInterface $time = null) → Ulid
```

### lnote-backend\vendor\symfony\uid\TimeBasedUidInterface.php
```
interface TimeBasedUidInterface
  function getDateTime() → \DateTimeImmutable
```

### lnote-backend\vendor\symfony\yaml\Parser.php
```
class Parser
  function parseFile(string $filename, int $flags = 0) → mixed
  function parse(string $value, int $flags = 0) → mixed
```

### lnote-backend\vendor\symfony\yaml\Inline.php
```
class Inline
  static function initialize(int $flags, ?int $parsedLineNumber = null, ?string $parsedFilename = null) → void
  static function parse(string $value, int $flags = 0, array &$references = []) → mixed
  static function dump(mixed $value, int $flags = 0, bool $rootLevel = false) → string
```

### lnote-backend\vendor\symfony\yaml\Command\LintCommand.php
```
class LintCommand
  function configure() → void
  function execute(InputInterface $input, OutputInterface $output) → int
```

### lnote-backend\vendor\symfony\yaml\Dumper.php
```
class Dumper
  function dump(mixed $input, int $inline = 0, int $indent = 0, int $flags = 0) → string
```

### lnote-backend\vendor\symfony\yaml\Resources\bin\yaml-lint
```
function includeIfExists(string $file): bool
```

### lnote-backend\vendor\symfony\yaml\Unescaper.php
```
class Unescaper
  function unescapeSingleQuotedString(string $value) → string
  function unescapeDoubleQuotedString(string $value) → string
```

### lnote-backend\vendor\symfony\yaml\Yaml.php
```
class Yaml
  static function parseFile(string $filename, int $flags = 0) → mixed
  static function parse(string $input, int $flags = 0) → mixed
  static function dump(mixed $input, int $inline = 2, int $indent = 4, int $flags = 0) → string
```

### lnote-backend\vendor\symfony\yaml\Exception\DumpException.php
```
class DumpException
```

### lnote-backend\vendor\symfony\yaml\Escaper.php
```
class Escaper
  static function requiresDoubleQuoting(string $value) → bool
  static function escapeWithDoubleQuotes(string $value) → string
  static function requiresSingleQuoting(string $value) → bool
  static function escapeWithSingleQuotes(string $value) → string
```

### lnote-backend\vendor\symfony\yaml\Exception\ParseException.php
```
class ParseException
  function getSnippet() → string
  function setSnippet(string $snippet) → void
  function getParsedFile() → string
  function setParsedFile(string $parsedFile) → void
  function getParsedLine() → int
  function setParsedLine(int $parsedLine) → void
```

### lnote-backend\vendor\symfony\translation\Loader\XliffFileLoader.php
```
class XliffFileLoader
  function load(mixed $resource, string $locale, string $domain = 'messages') → MessageCatalogue
```

### lnote-backend\vendor\symfony\translation\Command\XliffLintCommand.php
```
class XliffLintCommand
  function configure() → void
  function execute(InputInterface $input, OutputInterface $output) → int
```

### lnote-backend\vendor\symfony\translation\Command\TranslationTrait.php
```
trait TranslationTrait
```

### lnote-backend\vendor\symfony\translation\Dumper\XliffFileDumper.php
```
class XliffFileDumper
  function formatCatalogue(MessageCatalogue $messages, string $domain, array $options = []) → string
  function getExtension() → string
```

### lnote-backend\vendor\symfony\translation\Loader\PoFileLoader.php
```
class PoFileLoader
  function loadResource(string $resource) → array
```

### lnote-backend\vendor\symfony\translation\Util\ArrayConverter.php
```
class ArrayConverter
  static function expandToTree(array $messages) → array
```

### lnote-backend\vendor\symfony\translation\Loader\MoFileLoader.php
```
class MoFileLoader
  function loadResource(string $resource) → array
```

### lnote-backend\vendor\symfony\translation\Exception\UnsupportedSchemeException.php
```
class UnsupportedSchemeException
```

### lnote-backend\vendor\symfony\translation\Translator.php
```
class Translator
  function setConfigCacheFactory(ConfigCacheFactoryInterface $configCacheFactory) → void
  function addLoader(string $format, LoaderInterface $loader) → void
  function addResource(string $format, mixed $resource, string $locale, ?string $domain = null) → void
  function setLocale(string $locale) → void
  function getLocale() → string
  function setFallbackLocales(array $locales) → void
  function getFallbackLocales() → array
  function addGlobalParameter(string $id, string|int|float|TranslatableInterface $value) → void
```

### lnote-backend\vendor\symfony\translation\Util\XliffUtils.php
```
class XliffUtils
  static function getVersionNumber(\DOMDocument $dom) → string
  static function validateSchema(\DOMDocument $dom) → array
  static function getErrorsAsString(array $xmlErrors) → string
```

### lnote-backend\vendor\symfony\translation\DependencyInjection\TranslatorPathsPass.php
```
class TranslatorPathsPass
  function process(ContainerBuilder $container) → void
  function processValue(mixed $value, bool $isRoot = false) → mixed
```

### lnote-backend\vendor\symfony\translation\DependencyInjection\TranslatorPass.php
```
class TranslatorPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\translation\Dumper\PoFileDumper.php
```
class PoFileDumper
  function formatCatalogue(MessageCatalogue $messages, string $domain, array $options = []) → string
  function getExtension() → string
```

### lnote-backend\vendor\symfony\translation\Catalogue\TargetOperation.php
```
class TargetOperation
  function processDomain(string $domain) → void
```

### lnote-backend\vendor\symfony\translation\Loader\QtFileLoader.php
```
class QtFileLoader
  function load(mixed $resource, string $locale, string $domain = 'messages') → MessageCatalogue
```

### lnote-backend\vendor\symfony\translation\MessageCatalogue.php
```
class MessageCatalogue
  function getLocale() → string
  function getDomains() → array
  function all(?string $domain = null) → array
  function set(string $id, string $translation, string $domain = 'messages') → void
  function has(string $id, string $domain = 'messages') → bool
  function defines(string $id, string $domain = 'messages') → bool
  function get(string $id, string $domain = 'messages') → string
  function replace(array $messages, string $domain = 'messages') → void
```

### lnote-backend\vendor\symfony\translation\Catalogue\MergeOperation.php
```
class MergeOperation
  function processDomain(string $domain) → void
```

### lnote-backend\vendor\symfony\translation\Dumper\FileDumper.php
```
class FileDumper
  function setRelativePathTemplate(string $relativePathTemplate) → void
  function dump(MessageCatalogue $messages, array $options = []) → void
```

### lnote-backend\vendor\symfony\translation\Dumper\IcuResFileDumper.php
```
class IcuResFileDumper
  function formatCatalogue(MessageCatalogue $messages, string $domain, array $options = []) → string
  function getExtension() → string
```

### lnote-backend\vendor\symfony\translation\Loader\CsvFileLoader.php
```
class CsvFileLoader
  function loadResource(string $resource) → array
```

### lnote-backend\vendor\symfony\translation\DependencyInjection\LoggingTranslatorPass.php
```
class LoggingTranslatorPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\translation\Loader\IcuDatFileLoader.php
```
class IcuDatFileLoader
  function load(mixed $resource, string $locale, string $domain = 'messages') → MessageCatalogue
```

### lnote-backend\vendor\symfony\translation\Formatter\IntlFormatter.php
```
class IntlFormatter
  function formatIntl(string $message, string $locale, array $parameters = []) → string
```

### lnote-backend\vendor\symfony\translation\Loader\ArrayLoader.php
```
class ArrayLoader
  function load(mixed $resource, string $locale, string $domain = 'messages') → MessageCatalogue
```

### lnote-backend\vendor\symfony\translation\Loader\FileLoader.php
```
class FileLoader
  function load(mixed $resource, string $locale, string $domain = 'messages') → MessageCatalogue
```

### lnote-backend\vendor\symfony\translation\Loader\IcuResFileLoader.php
```
class IcuResFileLoader
  function load(mixed $resource, string $locale, string $domain = 'messages') → MessageCatalogue
  function flatten(\ResourceBundle $rb, array &$messages = [], ?string $path = null) → array
```

### lnote-backend\vendor\symfony\translation\Catalogue\AbstractOperation.php
```
class AbstractOperation
  function getDomains() → array
  function getMessages(string $domain) → array
  function getNewMessages(string $domain) → array
  function getObsoleteMessages(string $domain) → array
  function getResult() → MessageCatalogueInterface
  function moveMessagesToIntlDomainsIfPossible(string $batch = self::ALL_BATCH) → void
```

### lnote-backend\vendor\symfony\translation\Command\TranslationLintCommand.php
```
class TranslationLintCommand
  function complete(CompletionInput $input, CompletionSuggestions $suggestions) → void
  function configure() → void
  function initialize(InputInterface $input, OutputInterface $output) → void
  function execute(InputInterface $input, OutputInterface $output) → int
```

### lnote-backend\vendor\symfony\translation\Exception\MissingRequiredOptionException.php
```
class MissingRequiredOptionException
```

### lnote-backend\vendor\symfony\translation\Loader\JsonFileLoader.php
```
class JsonFileLoader
  function loadResource(string $resource) → array
```

### lnote-backend\vendor\symfony\translation\Loader\YamlFileLoader.php
```
class YamlFileLoader
  function loadResource(string $resource) → array
```

### lnote-backend\vendor\symfony\translation\Exception\IncompleteDsnException.php
```
class IncompleteDsnException
```

### lnote-backend\vendor\symfony\translation\Dumper\MoFileDumper.php
```
class MoFileDumper
  function formatCatalogue(MessageCatalogue $messages, string $domain, array $options = []) → string
  function getExtension() → string
```

### lnote-backend\vendor\symfony\translation\Resources\bin\translation-status.php
```
function findTranslationFiles($originalFilePath, $localeToAnalyze) → array
function calculateTranslationStatus($originalFilePath, $translationFilePaths) → array
function isTranslationCompleted(array $translationStatus) → bool
function printTranslationStatus($originalFilePath, $translationStatus, $verboseOutput, $includeCompletedLanguages)
function extractLocaleFromFilePath($filePath)
function extractTranslationKeys($filePath) → array
function findTransUnitMismatches(array $baseTranslationKeys, array $translatedKeys) → array
function printTitle($title)
function printTable($translations, $verboseOutput, bool $includeCompletedLanguages)
function textColorGreen()
function textColorRed()
function textColorNormal()
```

### lnote-backend\vendor\symfony\translation\Extractor\Visitor\AbstractVisitor.php
```
class AbstractVisitor
  function initialize(MessageCatalogue $catalogue, \SplFileInfo $file, string $messagePrefix) → void
  function addMessageToCatalogue(string $message, ?string $domain, int $line) → void
  function getStringArguments(Node\Expr\CallLike|Node\Attribute|Node\Expr\New_ $node, int|string $index, bool $indexIsRegex = false) → array
  function hasNodeNamedArguments(Node\Expr\CallLike|Node\Attribute|Node\Expr\New_ $node) → bool
  function nodeFirstNamedArgumentIndex(Node\Expr\CallLike|Node\Attribute|Node\Expr\New_ $node) → int
```

### lnote-backend\vendor\symfony\translation\Test\IncompleteDsnTestTrait.php
```
trait IncompleteDsnTestTrait
  function testIncompleteDsnException(string $dsn, ?string $message = null)
```

### lnote-backend\vendor\symfony\translation\Extractor\AbstractFileExtractor.php
```
class AbstractFileExtractor
  function extractFiles(string|iterable $resource) → iterable
  function isFile(string $file) → bool
```

### lnote-backend\vendor\symfony\translation\Reader\TranslationReader.php
```
class TranslationReader
  function addLoader(string $format, LoaderInterface $loader) → void
  function read(string $directory, MessageCatalogue $catalogue) → void
```

### lnote-backend\vendor\symfony\translation\Test\AbstractProviderFactoryTestCase.php
```
class AbstractProviderFactoryTestCase
  function testSupports(bool $expected, string $dsn)
  function testCreate(string $expected, string $dsn)
  function testUnsupportedSchemeException(string $dsn, ?string $message = null)
```

### lnote-backend\vendor\symfony\translation\Dumper\QtFileDumper.php
```
class QtFileDumper
  function formatCatalogue(MessageCatalogue $messages, string $domain, array $options = []) → string
  function getExtension() → string
```

### lnote-backend\vendor\symfony\translation\DependencyInjection\DataCollectorTranslatorPass.php
```
class DataCollectorTranslatorPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\translation\Dumper\YamlFileDumper.php
```
class YamlFileDumper
  function formatCatalogue(MessageCatalogue $messages, string $domain, array $options = []) → string
  function getExtension() → string
```

### lnote-backend\vendor\symfony\translation\Provider\TranslationProviderCollectionFactory.php
```
class TranslationProviderCollectionFactory
  function fromConfig(array $config) → TranslationProviderCollec
  function fromDsnObject(Dsn $dsn, array $locales, array $domains = []) → ProviderInterface
```

### lnote-backend\vendor\symfony\translation\DependencyInjection\TranslationDumperPass.php
```
class TranslationDumperPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\translation\DependencyInjection\TranslationExtractorPass.php
```
class TranslationExtractorPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\translation\Loader\PhpFileLoader.php
```
class PhpFileLoader
  function loadResource(string $resource) → array
```

### lnote-backend\vendor\symfony\translation\Writer\TranslationWriter.php
```
class TranslationWriter
  function addDumper(string $format, DumperInterface $dumper) → void
  function getFormats() → array
  function write(MessageCatalogue $catalogue, string $format, array $options = []) → void
```

### lnote-backend\vendor\symfony\translation\DataCollector\TranslationDataCollector.php
```
class TranslationDataCollector
  function lateCollect() → void
  function collect(Request $request, Response $response, ?\Throwable $exception = null) → void
  function reset() → void
  function getMessages() → array|Data
  function getCountMissings() → int
  function getCountFallbacks() → int
  function getCountDefines() → int
  function getLocale() → ?string
```

### lnote-backend\vendor\symfony\translation\Writer\TranslationWriterInterface.php
```
interface TranslationWriterInterface
  function write(MessageCatalogue $catalogue, string $format, array $options = []) → void
```

### lnote-backend\vendor\symfony\translation\LocaleSwitcher.php
```
class LocaleSwitcher
  function setLocale(string $locale) → void
  function getLocale() → string
  function runWithLocale(string $locale, callable $callback) → mixed
  function reset() → void
```

### lnote-backend\vendor\symfony\translation\Loader\LoaderInterface.php
```
interface LoaderInterface
  function load(mixed $resource, string $locale, string $domain = 'messages') → MessageCatalogue
```

### lnote-backend\vendor\symfony\translation\IdentityTranslator.php
```
class IdentityTranslator
  function setLocale(string $locale) → void
```

### lnote-backend\vendor\symfony\translation\Formatter\MessageFormatter.php
```
class MessageFormatter
  function format(string $message, string $locale, array $parameters = []) → string
  function formatIntl(string $message, string $locale, array $parameters = []) → string
```

### lnote-backend\vendor\symfony\translation\Dumper\DumperInterface.php
```
interface DumperInterface
  function dump(MessageCatalogue $messages, array $options = []) → void
```

### lnote-backend\vendor\symfony\translation\Exception\ProviderException.php
```
class ProviderException
  function getResponse() → ResponseInterface
  function getDebug() → string
```

### lnote-backend\vendor\symfony\translation\Formatter\MessageFormatterInterface.php
```
interface MessageFormatterInterface
  function format(string $message, string $locale, array $parameters = []) → string
```

### lnote-backend\vendor\symfony\translation\LoggingTranslator.php
```
class LoggingTranslator
  function trans(?string $id, array $parameters = [], ?string $domain = null, ?string $locale = null) → string
  function setLocale(string $locale) → void
  function getLocale() → string
  function getCatalogue(?string $locale = null) → MessageCatalogueInterface
  function getCatalogues() → array
  function getFallbackLocales() → array
```

### lnote-backend\vendor\symfony\translation\Formatter\IntlFormatterInterface.php
```
interface IntlFormatterInterface
  function formatIntl(string $message, string $locale, array $parameters = []) → string
```

### lnote-backend\vendor\symfony\translation\Reader\TranslationReaderInterface.php
```
interface TranslationReaderInterface
  function read(string $directory, MessageCatalogue $catalogue) → void
```

### lnote-backend\vendor\symfony\translation\Dumper\CsvFileDumper.php
```
class CsvFileDumper
  function formatCatalogue(MessageCatalogue $messages, string $domain, array $options = []) → string
  function setCsvControl(string $delimiter = ';', string $enclosure = '"') → void
  function getExtension() → string
```

### lnote-backend\vendor\symfony\translation\MessageCatalogueInterface.php
```
interface MessageCatalogueInterface
  function getLocale() → string
  function getDomains() → array
  function all(?string $domain = null) → array
  function set(string $id, string $translation, string $domain = 'messages') → void
  function has(string $id, string $domain = 'messages') → bool
  function defines(string $id, string $domain = 'messages') → bool
  function get(string $id, string $domain = 'messages') → string
  function replace(array $messages, string $domain = 'messages') → void
```

### lnote-backend\vendor\symfony\translation\Dumper\IniFileDumper.php
```
class IniFileDumper
  function formatCatalogue(MessageCatalogue $messages, string $domain, array $options = []) → string
  function getExtension() → string
```

### lnote-backend\vendor\symfony\translation\Extractor\ChainExtractor.php
```
class ChainExtractor
  function addExtractor(string $format, ExtractorInterface $extractor) → void
  function setPrefix(string $prefix) → void
  function extract(string|iterable $directory, MessageCatalogue $catalogue) → void
```

### lnote-backend\vendor\symfony\translation\Loader\IniFileLoader.php
```
class IniFileLoader
  function loadResource(string $resource) → array
```

### lnote-backend\vendor\symfony\translation\TranslatorBagInterface.php
```
interface TranslatorBagInterface
  function getCatalogue(?string $locale = null) → MessageCatalogueInterface
  function getCatalogues() → array
```

### lnote-backend\vendor\symfony\translation\Exception\ProviderExceptionInterface.php
```
interface ProviderExceptionInterface
  function getDebug() → string
```

### lnote-backend\vendor\symfony\translation\Extractor\ExtractorInterface.php
```
interface ExtractorInterface
  function extract(string|iterable $resource, MessageCatalogue $catalogue) → void
  function setPrefix(string $prefix) → void
```

### lnote-backend\vendor\symfony\translation\Provider\FilteringProvider.php
```
class FilteringProvider
  function write(TranslatorBagInterface $translatorBag) → void
  function read(array $domains, array $locales) → TranslatorBag
  function delete(TranslatorBagInterface $translatorBag) → void
  function getDomains() → array
```

### lnote-backend\vendor\symfony\translation\Dumper\JsonFileDumper.php
```
class JsonFileDumper
  function formatCatalogue(MessageCatalogue $messages, string $domain, array $options = []) → string
  function getExtension() → string
```

### lnote-backend\vendor\symfony\translation\CatalogueMetadataAwareInterface.php
```
interface CatalogueMetadataAwareInterface
  function getCatalogueMetadata(string $key = '', string $domain = 'messages') → mixed
  function setCatalogueMetadata(string $key, mixed $value, string $domain = 'messages') → void
  function deleteCatalogueMetadata(string $key = '', string $domain = 'messages') → void
```

### lnote-backend\vendor\symfony\translation\MetadataAwareInterface.php
```
interface MetadataAwareInterface
  function getMetadata(string $key = '', string $domain = 'messages') → mixed
  function setMetadata(string $key, mixed $value, string $domain = 'messages') → void
  function deleteMetadata(string $key = '', string $domain = 'messages') → void
```

### lnote-backend\vendor\symfony\translation\TranslatableMessage.php
```
class TranslatableMessage
  function getMessage() → string
  function getParameters() → array
  function getDomain() → ?string
  function trans(TranslatorInterface $translator, ?string $locale = null) → string
```

### lnote-backend\vendor\symfony\translation\Dumper\PhpFileDumper.php
```
class PhpFileDumper
  function formatCatalogue(MessageCatalogue $messages, string $domain, array $options = []) → string
  function getExtension() → string
```

### lnote-backend\vendor\symfony\translation\Test\ProviderTestCase.php
```
class ProviderTestCase
  function testToString(ProviderInterface $provider, string $expected)
  function getClient() → MockHttpClient
  function getLoader() → LoaderInterface
  function getLogger() → LoggerInterface
  function getDefaultLocale() → string
  function getXliffFileDumper() → XliffFileDumper
  function getTranslatorBag() → TranslatorBagInterface
```

### lnote-backend\vendor\symfony\translation\Catalogue\OperationInterface.php
```
interface OperationInterface
  function getDomains() → array
  function getMessages(string $domain) → array
  function getNewMessages(string $domain) → array
  function getObsoleteMessages(string $domain) → array
  function getResult() → MessageCatalogueInterface
```

### lnote-backend\vendor\symfony\translation\README.md
```
code-fence bash
code-fence plain
code-fence php
code-fence sponsor
```

### lnote-backend\vendor\symfony\translation\Provider\NullProvider.php
```
class NullProvider
  function write(TranslatorBagInterface $translatorBag, bool $override = false) → void
  function read(array $domains, array $locales) → TranslatorBag
  function delete(TranslatorBagInterface $translatorBag) → void
```

### lnote-backend\vendor\symfony\translation\Provider\AbstractProviderFactory.php
```
class AbstractProviderFactory
  function supports(Dsn $dsn) → bool
  function getUser(Dsn $dsn) → string
  function getPassword(Dsn $dsn) → string
```

### lnote-backend\vendor\symfony\translation\Provider\ProviderFactoryInterface.php
```
interface ProviderFactoryInterface
  function create(Dsn $dsn) → ProviderInterface
  function supports(Dsn $dsn) → bool
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\RequestDataCollector.php
```
class RequestDataCollector
  function collect(Request $request, Response $response, ?\Throwable $exception = null) → void
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\RegisterControllerArgumentLocatorsPass.php
```
class RegisterControllerArgumentLocatorsPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\http-kernel\Kernel.php
```
class Kernel
  function boot() → void
  function reboot(?string $warmupDir) → void
  function terminate(Request $request, Response $response) → void
  function shutdown() → void
  function handle(Request $request, int $type = HttpKernelInterface::MAIN_REQUEST, bool $catch = true) → Response
  function getHttpKernel() → HttpKernelInterface
  function getBundles() → array
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\AbstractSessionListener.php
```
class AbstractSessionListener
  function onKernelRequest(RequestEvent $event) → void
  function onKernelResponse(ResponseEvent $event) → void
```

### lnote-backend\vendor\symfony\http-kernel\HttpCache\HttpCache.php
```
class HttpCache
  function getStore() → StoreInterface
  function getTraces() → array
  function getLog() → string
  function getRequest() → Request
  function getKernel() → HttpKernelInterface
  function getSurrogate() → SurrogateInterface
  function handle(Request $request, int $type = HttpKernelInterface::MAIN_REQUEST, bool $catch = true) → Response
```

### lnote-backend\vendor\symfony\http-kernel\Controller\ArgumentResolver\RequestPayloadValueResolver.php
```
class RequestPayloadValueResolver
  function resolve(Request $request, ArgumentMetadata $argument) → iterable
  function onKernelControllerArguments(ControllerArgumentsEvent $event) → void
```

### lnote-backend\vendor\symfony\http-kernel\HttpKernel.php
```
class HttpKernel
  function handle(Request $request, int $type = HttpKernelInterface::MAIN_REQUEST, bool $catch = true) → Response
  function terminate(Request $request, Response $response) → void
  function terminateWithException(\Throwable $exception, ?Request $request = null) → void
```

### lnote-backend\vendor\symfony\http-kernel\HttpCache\ResponseCacheStrategy.php
```
class ResponseCacheStrategy
  function add(Response $response) → void
  function update(Response $response) → void
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\CacheAttributeListener.php
```
class CacheAttributeListener
  function onKernelControllerArguments(ControllerArgumentsEvent $event) → void
  function onKernelResponse(ResponseEvent $event) → void
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\AddAnnotatedClassesToCachePass.php
```
class AddAnnotatedClassesToCachePass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\http-kernel\HttpCache\Store.php
```
class Store
  function cleanup() → void
  function lock(Request $request) → bool|string
  function unlock(Request $request) → bool
  function isLocked(Request $request) → bool
  function lookup(Request $request) → ?Response
  function write(Request $request, Response $response) → string
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\DumpDataCollector.php
```
class DumpDataCollector
  function dump(Data $data) → ?string
  function collect(Request $request, Response $response, ?\Throwable $exception = null) → void
  function reset() → void
```

### lnote-backend\vendor\symfony\http-kernel\Profiler\FileProfilerStorage.php
```
class FileProfilerStorage
  function find(?string $ip, ?string $url, ?int $limit, ?string $method, ?int $start = null, ?int $end = null, ?string $statusCode = null, ?\Closure $filter = null) → array
  function purge() → void
  function read(string $token) → ?Profile
  function write(Profile $profile) → bool
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\RouterListener.php
```
class RouterListener
  function onKernelFinishRequest() → void
  function onKernelRequest(RequestEvent $event) → void
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\ErrorListener.php
```
class ErrorListener
  function logKernelException(ExceptionEvent $event) → void
  function onKernelException(ExceptionEvent $event) → void
  function removeCspHeader(ResponseEvent $event) → void
  function onControllerArguments(ControllerArgumentsEvent $event) → void
```

### lnote-backend\vendor\symfony\http-kernel\Controller\ControllerResolver.php
```
class ControllerResolver
  function allowControllers(array $types = [], array $attributes = []) → void
  function getController(Request $request) → callable|false
  function createController(string $controller) → callable
  function instantiateController(string $class) → object
```

### lnote-backend\vendor\symfony\http-kernel\Debug\ErrorHandlerConfigurator.php
```
class ErrorHandlerConfigurator
  function configure(ErrorHandler $handler) → void
```

### lnote-backend\vendor\symfony\http-kernel\Fragment\AbstractSurrogateFragmentRenderer.php
```
class AbstractSurrogateFragmentRenderer
  function render(string|ControllerReference $uri, Request $request, array $options = []) → Response
```

### lnote-backend\vendor\symfony\http-kernel\Attribute\MapRequestPayload.php
```
class MapRequestPayload
```

### lnote-backend\vendor\symfony\http-kernel\HttpCache\SubRequestHandler.php
```
class SubRequestHandler
  static function handle(HttpKernelInterface $kernel, Request $request, int $type, bool $catch) → Response
```

### lnote-backend\vendor\symfony\http-kernel\Controller\ControllerReference.php
```
class ControllerReference
```

### lnote-backend\vendor\symfony\http-kernel\Attribute\MapQueryString.php
```
class MapQueryString
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\DebugHandlersListener.php
```
class DebugHandlersListener
  function configure(?object $event = null) → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\DataCollector.php
```
class DataCollector
  function cloneVar(mixed $var) → Data
  function getCasters() → array
  function reset()
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\ConfigurableExtension.php
```
class ConfigurableExtension
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\LoggerDataCollector.php
```
class LoggerDataCollector
  function collect(Request $request, Response $response, ?\Throwable $exception = null) → void
  function lateCollect() → void
  function getLogs() → Data|array
  function getProcessedLogs() → array
  function getFilters() → array
  function getPriorities() → Data|array
  function countErrors() → int
  function countDeprecations() → int
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\RemoveEmptyControllerArgumentLocatorsPass.php
```
class RemoveEmptyControllerArgumentLocatorsPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\http-kernel\Attribute\MapDateTime.php
```
class MapDateTime
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\ControllerArgumentValueResolverPass.php
```
class ControllerArgumentValueResolverPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\http-kernel\Fragment\InlineFragmentRenderer.php
```
class InlineFragmentRenderer
  function render(string|ControllerReference $uri, Request $request, array $options = []) → Response
  function createSubRequest(string $uri, Request $request) → Request
  function getName() → string
```

### lnote-backend\vendor\symfony\http-kernel\Attribute\MapUploadedFile.php
```
class MapUploadedFile
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\ConfigDataCollector.php
```
class ConfigDataCollector
  function setKernel(KernelInterface $kernel) → void
  function collect(Request $request, Response $response, ?\Throwable $exception = null) → void
  function lateCollect() → void
  function getToken() → ?string
  function getSymfonyVersion() → string
  function getSymfonyState() → string
  function getSymfonyMinorVersion() → string
  function isSymfonyLts() → bool
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\ResettableServicePass.php
```
class ResettableServicePass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\http-kernel\Exception\TooManyRequestsHttpException.php
```
class TooManyRequestsHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Attribute\ValueResolver.php
```
class ValueResolver
```

### lnote-backend\vendor\symfony\http-kernel\Attribute\WithHttpStatus.php
```
class WithHttpStatus
```

### lnote-backend\vendor\symfony\http-kernel\Debug\TraceableEventDispatcher.php
```
class TraceableEventDispatcher
  function beforeDispatch(string $eventName, object $event) → void
  function afterDispatch(string $eventName, object $event) → void
```

### lnote-backend\vendor\symfony\http-kernel\Controller\ContainerControllerResolver.php
```
class ContainerControllerResolver
  function instantiateController(string $class) → object
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\ServicesResetter.php
```
class ServicesResetter
  function reset() → void
```

### lnote-backend\vendor\symfony\http-kernel\Exception\ServiceUnavailableHttpException.php
```
class ServiceUnavailableHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Log\Logger.php
```
class Logger
  function enableDebug() → void
  function log($level, $message, array $context = []) → void
  function getLogs(?Request $request = null) → array
  function countErrors(?Request $request = null) → int
  function clear() → void
```

### lnote-backend\vendor\symfony\http-kernel\Controller\ErrorController.php
```
class ErrorController
  function preview(Request $request, int $code) → Response
```

### lnote-backend\vendor\symfony\http-kernel\Exception\MethodNotAllowedHttpException.php
```
class MethodNotAllowedHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Exception\UnauthorizedHttpException.php
```
class UnauthorizedHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Profiler\Profile.php
```
class Profile
  function setToken(string $token) → void
  function getToken() → string
  function setParent(self $parent) → void
  function getParent() → ?self
  function getParentToken() → ?string
  function getIp() → ?string
  function setIp(?string $ip) → void
  function getMethod() → ?string
```

### lnote-backend\vendor\symfony\http-kernel\HttpCache\Ssi.php
```
class Ssi
  function getName() → string
  function addSurrogateControl(Response $response) → void
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\FragmentRendererPass.php
```
class FragmentRendererPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\http-kernel\Fragment\FragmentHandler.php
```
class FragmentHandler
  function addRenderer(FragmentRendererInterface $renderer) → void
  function render(string|ControllerReference $uri, string $renderer = 'inline', array $options = []) → ?string
  function deliver(Response $response) → ?string
```

### lnote-backend\vendor\symfony\http-kernel\Profiler\Profiler.php
```
class Profiler
  function disable() → void
  function enable() → void
  function isEnabled() → bool
  function loadProfileFromResponse(Response $response) → ?Profile
  function loadProfile(string $token) → ?Profile
  function saveProfile(Profile $profile) → bool
  function purge() → void
  function find(?string $ip, ?string $url, ?int $limit, ?string $method, ?string $start, ?string $end, ?string $statusCode = null, ?\Closure $filter = null) → array
```

### lnote-backend\vendor\symfony\http-kernel\Attribute\AsTargetedValueResolver.php
```
class AsTargetedValueResolver
```

### lnote-backend\vendor\symfony\http-kernel\CacheWarmer\CacheWarmerAggregate.php
```
class CacheWarmerAggregate
  function enableOptionalWarmers() → void
  function enableOnlyOptionalWarmers() → void
  function warmUp(string $cacheDir, ?string $buildDir = null, ?SymfonyStyle $io = null) → array
  function isOptional() → bool
```

### lnote-backend\vendor\symfony\http-kernel\Exception\PreconditionRequiredHttpException.php
```
class PreconditionRequiredHttpException
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\ProfilerListener.php
```
class ProfilerListener
  function onKernelException(ExceptionEvent $event) → void
  function onKernelResponse(ResponseEvent $event) → void
  function onKernelTerminate(TerminateEvent $event) → void
  function reset() → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\Attribute\AsController.php
```
class AsController
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\RegisterLocaleAwareServicesPass.php
```
class RegisterLocaleAwareServicesPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\DumpListener.php
```
class DumpListener
  function configure() → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\Exception\AccessDeniedHttpException.php
```
class AccessDeniedHttpException
```

### lnote-backend\vendor\symfony\http-kernel\HttpKernelBrowser.php
```
class HttpKernelBrowser
  function catchExceptions(bool $catchExceptions) → void
  function doRequest(object $request) → Response
  function getScript(object $request) → string
  function getHandleScript() → string
  function filterRequest(DomRequest $request) → Request
  function filterFiles(array $files) → array
  function filterResponse(object $response) → DomResponse
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\LazyLoadingFragmentHandler.php
```
class LazyLoadingFragmentHandler
  function render(string|ControllerReference $uri, string $renderer = 'inline', array $options = []) → ?string
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\FragmentListener.php
```
class FragmentListener
  function onKernelRequest(RequestEvent $event) → void
  function validateRequest(Request $request) → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\LoggerPass.php
```
class LoggerPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\http-kernel\Exception\BadRequestHttpException.php
```
class BadRequestHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Exception\ConflictHttpException.php
```
class ConflictHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Exception\GoneHttpException.php
```
class GoneHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Exception\LengthRequiredHttpException.php
```
class LengthRequiredHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Exception\LockedHttpException.php
```
class LockedHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Exception\NotAcceptableHttpException.php
```
class NotAcceptableHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Exception\NotFoundHttpException.php
```
class NotFoundHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Exception\PreconditionFailedHttpException.php
```
class PreconditionFailedHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Exception\UnprocessableEntityHttpException.php
```
class UnprocessableEntityHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Exception\UnsupportedMediaTypeHttpException.php
```
class UnsupportedMediaTypeHttpException
```

### lnote-backend\vendor\symfony\http-kernel\Fragment\HIncludeFragmentRenderer.php
```
class HIncludeFragmentRenderer
  function hasTemplating() → bool
  function render(string|ControllerReference $uri, Request $request, array $options = []) → Response
  function getName() → string
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\ServicesResetterInterface.php
```
interface ServicesResetterInterface
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\ResponseListener.php
```
class ResponseListener
  function onKernelResponse(ResponseEvent $event) → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\SurrogateListener.php
```
class SurrogateListener
  function onKernelResponse(ResponseEvent $event) → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\MergeExtensionConfigurationPass.php
```
class MergeExtensionConfigurationPass
  function process(ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\SessionListener.php
```
class SessionListener
  function getSession() → ?SessionInterface
```

### lnote-backend\vendor\symfony\http-kernel\Controller\ControllerResolverInterface.php
```
interface ControllerResolverInterface
  function getController(Request $request) → callable|false
```

### lnote-backend\vendor\symfony\http-kernel\HttpCache\Esi.php
```
class Esi
  function getName() → string
  function addSurrogateControl(Response $response) → void
  function renderIncludeTag(string $uri, ?string $alt = null, bool $ignoreErrors = true, string $comment = '') → string
  function process(Request $request, Response $response) → Response
```

### lnote-backend\vendor\symfony\http-kernel\Config\FileLocator.php
```
class FileLocator
  function locate(string $file, ?string $currentPath = null, bool $first = true) → string|array
```

### lnote-backend\vendor\symfony\http-kernel\HttpKernelInterface.php
```
interface HttpKernelInterface
  function handle(Request $request, int $type = self::MAIN_REQUEST, bool $catch = true) → Response
```

### lnote-backend\vendor\symfony\http-kernel\Controller\TraceableArgumentResolver.php
```
class TraceableArgumentResolver
  function getArguments(Request $request, callable $controller, ?\ReflectionFunctionAbstract $reflector = null) → array
```

### lnote-backend\vendor\symfony\http-kernel\Controller\TraceableControllerResolver.php
```
class TraceableControllerResolver
  function getController(Request $request) → callable|false
```

### lnote-backend\vendor\symfony\http-kernel\CacheClearer\ChainCacheClearer.php
```
class ChainCacheClearer
  function clear(string $cacheDir) → void
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\LocaleAwareListener.php
```
class LocaleAwareListener
  function onKernelRequest(RequestEvent $event) → void
  function onKernelFinishRequest(FinishRequestEvent $event) → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\Log\DebugLoggerConfigurator.php
```
class DebugLoggerConfigurator
  function pushDebugLogger(Logger $logger) → void
  static function getDebugLogger(mixed $logger) → ?DebugLoggerInterface
```

### lnote-backend\vendor\symfony\http-kernel\DependencyInjection\Extension.php
```
class Extension
  function getAnnotatedClassesToCompile() → array
  function addAnnotatedClassesToCompile(array $annotatedClasses) → void
```

### lnote-backend\vendor\symfony\http-kernel\HttpCache\AbstractSurrogate.php
```
class AbstractSurrogate
  function createCacheStrategy() → ResponseCacheStrategyInte
  function hasSurrogateCapability(Request $request) → bool
  function addSurrogateCapability(Request $request) → void
  function needsParsing(Response $response) → bool
  function handle(HttpCache $cache, string $uri, string $alt, bool $ignoreErrors) → string
  function removeFromControl(Response $response) → void
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\IsSignatureValidAttributeListener.php
```
class IsSignatureValidAttributeListener
  function onKernelControllerArguments(ControllerArgumentsEvent $event) → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\LocaleListener.php
```
class LocaleListener
  function setDefaultLocale(KernelEvent $event) → void
  function onKernelRequest(RequestEvent $event) → void
  function onKernelFinishRequest(FinishRequestEvent $event) → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\ValidateRequestListener.php
```
class ValidateRequestListener
  function onKernelRequest(RequestEvent $event) → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\Bundle\Bundle.php
```
class Bundle
  function boot()
  function shutdown()
  function build(ContainerBuilder $container)
  function getContainerExtension() → ?ExtensionInterface
  function getNamespace() → string
  function getPath() → string
  function registerCommands(Application $application)
  function getContainerExtensionClass() → string
```

### lnote-backend\vendor\symfony\http-kernel\Fragment\RoutableFragmentRenderer.php
```
class RoutableFragmentRenderer
  function setFragmentPath(string $path) → void
  function generateFragmentUri(ControllerReference $reference, Request $request, bool $absolute = false, bool $strict = true) → string
```

### lnote-backend\vendor\symfony\http-kernel\CacheWarmer\CacheWarmer.php
```
class CacheWarmer
  function writeCacheFile(string $file, $content) → void
```

### lnote-backend\vendor\symfony\http-kernel\Fragment\FragmentUriGeneratorInterface.php
```
interface FragmentUriGeneratorInterface
  function generate(ControllerReference $controller, ?Request $request = null, bool $absolute = false, bool $strict = true, bool $sign = true) → string
```

### lnote-backend\vendor\symfony\http-kernel\TerminableInterface.php
```
interface TerminableInterface
  function terminate(Request $request, Response $response) → void
```

### lnote-backend\vendor\symfony\http-kernel\KernelInterface.php
```
interface KernelInterface
  function registerBundles() → iterable
  function registerContainerConfiguration(LoaderInterface $loader)
  function boot()
  function shutdown()
  function getBundles() → array
  function getBundle(string $name) → BundleInterface
  function locateResource(string $name) → string
  function getEnvironment() → string
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\AddRequestFormatsListener.php
```
class AddRequestFormatsListener
  function onKernelRequest(RequestEvent $event) → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\ControllerMetadata\ArgumentMetadata.php
```
class ArgumentMetadata
  function getName() → string
  function getType() → ?string
  function isVariadic() → bool
  function hasDefaultValue() → bool
  function isNullable() → bool
  function getDefaultValue() → mixed
  function getAttributes(?string $name = null, int $flags = 0) → array
  function getAttributesOfType(string $name, int $flags = 0) → array
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\EventDataCollector.php
```
class EventDataCollector
  function collect(Request $request, Response $response, ?\Throwable $exception = null) → void
  function reset() → void
  function lateCollect() → void
  function getData() → array|Data
  function setCalledListeners(array $listeners, ?string $dispatcher = null) → void
  function getCalledListeners(?string $dispatcher = null) → array|Data
  function setNotCalledListeners(array $listeners, ?string $dispatcher = null) → void
  function getNotCalledListeners(?string $dispatcher = null) → array|Data
```

### lnote-backend\vendor\symfony\http-kernel\CacheWarmer\CacheWarmerInterface.php
```
interface CacheWarmerInterface
  function isOptional() → bool
```

### lnote-backend\vendor\symfony\http-kernel\CacheWarmer\WarmableInterface.php
```
interface WarmableInterface
  function warmUp(string $cacheDir, ?string $buildDir = null) → array
```

### lnote-backend\vendor\symfony\http-kernel\Controller\ArgumentResolverInterface.php
```
interface ArgumentResolverInterface
  function getArguments(Request $request, callable $controller, ?\ReflectionFunctionAbstract $reflector = null) → array
```

### lnote-backend\vendor\symfony\http-kernel\RebootableInterface.php
```
interface RebootableInterface
  function reboot(?string $warmupDir) → void
```

### lnote-backend\vendor\symfony\http-kernel\Controller\ValueResolverInterface.php
```
interface ValueResolverInterface
  function resolve(Request $request, ArgumentMetadata $argument) → iterable
```

### lnote-backend\vendor\symfony\http-kernel\Event\RequestEvent.php
```
class RequestEvent
  function getResponse() → ?Response
  function setResponse(Response $response) → void
  function hasResponse() → bool
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\TimeDataCollector.php
```
class TimeDataCollector
  function collect(Request $request, Response $response, ?\Throwable $exception = null) → void
  function reset() → void
  function lateCollect() → void
  function setEvents(array $events) → void
  function getEvents() → array
  function getDuration() → float
  function getInitTime() → float
  function getStartTime() → float
```

### lnote-backend\vendor\symfony\http-kernel\Event\KernelEvent.php
```
class KernelEvent
  function getKernel() → HttpKernelInterface
  function getRequest() → Request
  function getRequestType() → int
  function isMainRequest() → bool
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\LateDataCollectorInterface.php
```
interface LateDataCollectorInterface
  function lateCollect()
```

### lnote-backend\vendor\symfony\http-kernel\EventListener\DisallowRobotsIndexingListener.php
```
class DisallowRobotsIndexingListener
  function onResponse(ResponseEvent $event) → void
  static function getSubscribedEvents() → array
```

### lnote-backend\vendor\symfony\http-kernel\Bundle\BundleExtension.php
```
class BundleExtension
  function getConfiguration(array $config, ContainerBuilder $container) → ?ConfigurationInterface
  function getAlias() → string
  function prepend(ContainerBuilder $container) → void
  function load(array $configs, ContainerBuilder $container) → void
```

### lnote-backend\vendor\symfony\http-kernel\CacheClearer\Psr6CacheClearer.php
```
class Psr6CacheClearer
  function hasPool(string $name) → bool
  function getPool(string $name) → CacheItemPoolInterface
  function clearPool(string $name) → bool
  function clear(string $cacheDir) → void
```

### lnote-backend\vendor\symfony\http-kernel\CacheClearer\CacheClearerInterface.php
```
interface CacheClearerInterface
  function clear(string $cacheDir) → void
```

### lnote-backend\vendor\symfony\http-kernel\ControllerMetadata\ArgumentMetadataFactoryInterface.php
```
interface ArgumentMetadataFactoryInterface
  function createArgumentMetadata(string|object|array $controller, ?\ReflectionFunctionAbstract $reflector = null) → array
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\DataCollectorInterface.php
```
interface DataCollectorInterface
  function collect(Request $request, Response $response, ?\Throwable $exception = null)
  function getName()
```

### lnote-backend\vendor\symfony\http-kernel\Fragment\EsiFragmentRenderer.php
```
class EsiFragmentRenderer
  function getName() → string
```

### lnote-backend\vendor\symfony\http-kernel\Fragment\SsiFragmentRenderer.php
```
class SsiFragmentRenderer
  function getName() → string
```

### lnote-backend\vendor\symfony\http-kernel\HttpCache\ResponseCacheStrategyInterface.php
```
interface ResponseCacheStrategyInterface
  function add(Response $response) → void
  function update(Response $response) → void
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\MemoryDataCollector.php
```
class MemoryDataCollector
  function collect(Request $request, Response $response, ?\Throwable $exception = null) → void
  function reset() → void
  function lateCollect() → void
  function getMemory() → int
  function getMemoryLimit() → int|float
  function updateMemoryUsage() → void
  function getName() → string
```

### lnote-backend\vendor\symfony\http-kernel\Log\DebugLoggerInterface.php
```
interface DebugLoggerInterface
  function getLogs(?Request $request = null) → array
  function countErrors(?Request $request = null) → int
  function clear() → void
```

### lnote-backend\vendor\symfony\http-kernel\Fragment\FragmentRendererInterface.php
```
interface FragmentRendererInterface
  function render(string|ControllerReference $uri, Request $request, array $options = []) → Response
  function getName() → string
```

### lnote-backend\vendor\symfony\http-kernel\Profiler\ProfilerStorageInterface.php
```
interface ProfilerStorageInterface
  function find(?string $ip, ?string $url, ?int $limit, ?string $method, ?int $start = null, ?int $end = null, ?string $statusCode = null, ?\Closure $filter = null) → array
  function read(string $token) → ?Profile
  function write(Profile $profile) → bool
  function purge() → void
```

### lnote-backend\vendor\symfony\http-kernel\Profiler\ProfilerStateChecker.php
```
class ProfilerStateChecker
  function isProfilerEnabled() → bool
  function isProfilerDisabled() → bool
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\RouterDataCollector.php
```
class RouterDataCollector
  function collect(Request $request, Response $response, ?\Throwable $exception = null) → void
  function reset() → void
  function guessRoute(Request $request, string|object|array $controller) → string
  function onKernelController(ControllerEvent $event) → void
  function getRedirect() → bool
  function getTargetUrl() → ?string
  function getTargetRoute() → ?string
  function getName() → string
```

### lnote-backend\vendor\symfony\http-kernel\Bundle\AbstractBundle.php
```
class AbstractBundle
  function configure(DefinitionConfigurator $definition) → void
  function prependExtension(ContainerConfigurator $configurator, ContainerBuilder $container) → void
  function loadExtension(array $config, ContainerConfigurator $configurator, ContainerBuilder $container) → void
  function getContainerExtension() → ?ExtensionInterface
  function getPath() → string
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\AjaxDataCollector.php
```
class AjaxDataCollector
  function collect(Request $request, Response $response, ?\Throwable $exception = null) → void
  function reset() → void
  function getName() → string
```

### lnote-backend\vendor\symfony\http-kernel\HttpCache\StoreInterface.php
```
interface StoreInterface
  function lookup(Request $request) → ?Response
  function write(Request $request, Response $response) → string
  function invalidate(Request $request) → void
  function lock(Request $request) → bool|string
  function unlock(Request $request) → bool
  function isLocked(Request $request) → bool
  function purge(string $url) → bool
  function cleanup() → void
```

### lnote-backend\vendor\symfony\http-kernel\HttpCache\SurrogateInterface.php
```
interface SurrogateInterface
  function getName() → string
  function createCacheStrategy() → ResponseCacheStrategyInte
  function hasSurrogateCapability(Request $request) → bool
  function addSurrogateCapability(Request $request) → void
  function addSurrogateControl(Response $response) → void
  function needsParsing(Response $response) → bool
  function renderIncludeTag(string $uri, ?string $alt = null, bool $ignoreErrors = true, string $comment = '') → string
  function process(Request $request, Response $response) → Response
```

### lnote-backend\vendor\symfony\http-kernel\Bundle\BundleInterface.php
```
interface BundleInterface
  function boot()
  function shutdown()
  function build(ContainerBuilder $container)
  function getContainerExtension() → ?ExtensionInterface
  function getName() → string
  function getNamespace() → string
  function getPath() → string
  function setContainer(?ContainerInterface $container) → void
```

### lnote-backend\vendor\symfony\http-kernel\DataCollector\ExceptionDataCollector.php
```
class ExceptionDataCollector
  function collect(Request $request, Response $response, ?\Throwable $exception = null) → void
  function hasException() → bool
  function getException() → \Exception|FlattenExcepti
  function getMessage() → string
  function getCode() → int|string
  function getStatusCode() → int
  function getTrace() → array
  function getName() → string
```

### lnote-backend\vendor\nette\utils\src\SmartObject.php
```
trait SmartObject
```

### lnote-backend\vendor\nette\utils\src\Utils\Image.php
```
class Image
  static function rgb(int $red, int $green, int $blue, int $transparency = 0) → array
  static function fromFile(string $file, ?int &$type = null, ?string &$warnings = self::Sentinel) → static
  static function fromString(string $s, ?int &$type = null, ?string &$warnings = self::Sentinel) → static
  static function fromBlank(int $width, int $height, ImageColor|array|null $color = null) → static
  static function detectTypeFromFile(string $file, mixed &$width = null, mixed &$height = null) → ?int
  static function detectTypeFromString(string $s, mixed &$width = null, mixed &$height = null) → ?int
  static function typeToExtension(int $type) → string
```

### lnote-backend\vendor\nette\utils\src\Utils\Validators.php
```
class Validators
  static function assert(mixed $value, string $expected, string $label = 'variable') → void
  static function assertField(array $array, int|string $key, ?string $expected = null, string $label = "item '%' in array",) → void
  static function is(mixed $value, string $expected) → bool
```

### lnote-backend\vendor\nette\utils\src\Utils\Strings.php
```
class Strings
  static function checkEncoding(string $s) → bool
  static function fixEncoding(string $s) → string
  static function chr(int $code) → string
  static function ord(string $c) → int
  static function startsWith(string $haystack, string $needle) → bool
  static function endsWith(string $haystack, string $needle) → bool
  static function contains(string $haystack, string $needle) → bool
  static function substring(string $s, int $start, ?int $length = null) → string
```

### lnote-backend\vendor\nette\utils\src\Utils\exceptions.php
```
class UnknownImageFileException
```

### lnote-backend\vendor\nette\utils\src\Utils\Arrays.php
```
class Arrays
  static function get(array $array, string|int|array $key, mixed $default = null) → mixed
  static function mergeTree(array $array1, array $array2) → array
  static function getKeyOffset(array $array, string|int $key) → ?int
  static function searchKey(array $array, string|int $key) → ?int
  static function contains(array $array, mixed $value) → bool
  static function first(array $array, ?callable $predicate = null, ?callable $else = null) → mixed
  static function last(array $array, ?callable $predicate = null, ?callable $else = null) → mixed
  static function firstKey(array $array, ?callable $predicate = null) → int|string|null
```

### lnote-backend\vendor\nette\utils\readme.md
```
code-fence plain
```

### lnote-backend\vendor\nette\utils\src\exceptions.php
```
class DeprecatedException
class FileNotFoundException
class DirectoryNotFoundException
```

### lnote-backend\vendor\nette\utils\src\HtmlStringable.php
```
interface HtmlStringable
```

### lnote-backend\vendor\nette\utils\src\Translator.php
```
interface Translator
```

### lnote-backend\vendor\nette\utils\src\Utils\Paginator.php
```
class Paginator
  function setPage(int $page) → static
  function getPage() → int
  function getFirstPage() → int
  function getLastPage() → ?int
  function getFirstItemOnPage() → int
  function getLastItemOnPage() → int
  function setBase(int $base) → static
  function getBase() → int
```

### lnote-backend\vendor\nette\utils\src\StaticClass.php
```
trait StaticClass
```

### lnote-backend\vendor\nette\utils\src\Utils\ImageColor.php
```
class ImageColor
  static function rgb(int $red, int $green, int $blue, float $opacity = 1) → self
  static function hex(string $hex) → self
  function toRGBA() → array
```

### lnote-backend\vendor\nette\utils\src\Utils\Helpers.php
```
class Helpers
  static function capture(callable $func) → string
  static function getLastError() → string
  static function falseToNull(mixed $value) → mixed
  static function clamp(int|float $value, int|float $min, int|float $max) → int|float
  static function getSuggestion(array $possibilities, string $value) → ?string
  static function compare(mixed $left, string $operator, mixed $right) → bool
  static function splitClassName(string $name) → array
```

### lnote-backend\vendor\nette\utils\src\Utils\Floats.php
```
class Floats
  static function isZero(float $value) → bool
  static function isInteger(float $value) → bool
  static function compare(float $a, float $b) → int
  static function areEqual(float $a, float $b) → bool
  static function isLessThan(float $a, float $b) → bool
  static function isLessThanOrEqualTo(float $a, float $b) → bool
  static function isGreaterThan(float $a, float $b) → bool
  static function isGreaterThanOrEqualTo(float $a, float $b) → bool
```

### lnote-backend\README.md
```
h1 L-Note Backend — Laravel 11 + PostgreSQL
h2 🏗️ Project Structure
h2 🛠️ Tech Stack
h2 📦 Key Dependencies
h3 Core
h3 Database & ORM
h3 Services
h3 Testing
h2 🚀 Getting Started
h3 Prerequisites
h3 Installation
h1 Clone repository
h1 Install dependencies
h1 Copy environment file
h1 Generate app key
h1 Configure database in .env
h1 DB_CONNECTION=pgsql
h1 DB_HOST=127.0.0.1
h1 DB_PORT=5432
h1 DB_DATABASE=lnote
h1 DB_USERNAME=postgres
h1 DB_PASSWORD=
h1 Run migrations
h1 Seed default data
h1 Create storage symlink
```

### lnote-backend\vendor\composer\InstalledVersions.php
```
class InstalledVersions
  static function getInstalledPackages()
  static function getInstalledPackagesByType($type)
  static function isInstalled($packageName, $includeDevRequirements = true)
  static function satisfies(VersionParser $parser, $packageName, $constraint)
  static function getVersionRanges($packageName)
  static function getVersion($packageName)
  static function getPrettyVersion($packageName)
```

### lnote-backend\vendor\composer\autoload_static.php
```
class ComposerStaticInit626b9e7ddd47fb7eff9aaa53cce0c9ad
```

### lnote-backend\vendor\composer\autoload_real.php
```
class ComposerAutoloaderInit626b9e7ddd47fb7eff9aaa53cce0c9ad
  static function loadClassLoader($class)
  static function getLoader()
```

### lnote-backend\app\Models\Customer.php
```
class Customer
  function transactions()
```

### lnote-backend\app\Models\ServicePrice.php
```
class ServicePrice
  function transactions()
```

### lnote-backend\app\Models\Transaction.php
```
class Transaction
  function user()
  function customer()
  function servicePrice()
```

### lnote-backend\database\seeders\DatabaseSeeder.php
```
class DatabaseSeeder
  function run() → void
```

### lnote-backend\database\seeders\ServicePriceSeeder.php
```
class ServicePriceSeeder
  function run() → void
```

### lnote-backend\database\seeders\CustomerSeeder.php
```
class CustomerSeeder
  function run() → void
```

### lnote-backend\app\Http\Controllers\Api\CustomerController.php
```
class CustomerController
  function index()
  function store(Request $request)
  function show($id)
  function update(Request $request, $id)
  function destroy($id)
```

### lnote-backend\app\Http\Controllers\Api\TransactionController.php
```
class TransactionController
  function index()
  function store(Request $request)
  function show($id)
  function update(Request $request, $id)
  function destroy($id)
```

### lnote-backend\.github\copilot-instructions.md
```
h2 Auto-generated signatures
h2 SigMap commands
h1 Code signatures
h2 app
h3 app\Http\Controllers\Api\CustomerController.php
h3 app\Http\Controllers\Api\TransactionController.php
h3 app\Http\Controllers\Controller.php
h3 app\Models\Customer.php
h3 app\Models\ServicePrice.php
h3 app\Models\Transaction.php
h3 app\Models\User.php
h3 app\Providers\AppServiceProvider.php
h2 app-src
h3 app-src\app\Http\Controllers\Controller.php
h3 app-src\app\Models\User.php
h3 app-src\app\Providers\AppServiceProvider.php
h3 app-src\database\factories\UserFactory.php
h3 app-src\database\seeders\DatabaseSeeder.php
h3 app-src\phpunit.xml
h3 app-src\README.md
h3 app-src\tests\Feature\ExampleTest.php
h3 app-src\tests\TestCase.php
h3 app-src\tests\Unit\ExampleTest.php
h2 database
h3 database\factories\UserFactory.php
```
